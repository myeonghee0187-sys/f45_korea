# F45 코리아 랜딩 페이지 현재 상태

마지막 업데이트: 2026-08-30

## 구현 완료

- **2026-08-30(히어로 5번 신규 세로 이미지 생성)**: `hero_frame_05.jpg`의 9명 인원 구성·뒷줄 8명 어깨동무·앞줄 중앙 1명 무릎 자세·운동복 색상·F45 로고와 체육관 배경을 참조해 새로운 모바일 단체 사진을 생성함. 아홉 명 전원이 머리부터 신발까지 프레임 안에 들어오는 세로 구도로 만들고 `assets/images/hero_frame_05_generated_430x600.jpg`로 별도 저장함. 기존 이미지와 캐러셀 연결은 변경하지 않음.

- **2026-08-30(히어로 4번 신규 세로 이미지 생성)**: `hero_frame_04.jpg`의 네 인물·좌우 순서·웃는 표정·어깨동무·운동복과 수건·F45 체육관 배경을 참조해 새로운 모바일 단체 사진을 생성함. 1차 결과가 허벅지까지 보여 재생성하고, 최종본은 머리부터 허리선까지만 보이는 타이트한 상반신 구도로 조정해 `assets/images/hero_frame_04_generated_430x600.jpg`로 별도 저장함. 기존 이미지와 캐러셀 연결은 변경하지 않음.

- **2026-08-30(히어로 3번 신규 세로 이미지 생성)**: `hero_frame_03.jpg`의 남성 배틀로프 운동·오른쪽 여성 덤벨 로우·배경의 그룹 운동·F45 체육관과 파란 그리드 바닥 콘셉트를 참조해 새로운 모바일 사진을 생성함. 주인공 남성의 전신과 여성의 운동 동작, 로프와 덤벨이 보이는 세로 구도로 만들고 `assets/images/hero_frame_03_generated_430x600.jpg`로 별도 저장함. 기존 이미지와 캐러셀 연결은 변경하지 않음.

- **2026-08-30(히어로 2번 신규 세로 이미지 생성)**: `hero_frame_02.jpg`의 젊은 여성 코치·중년 여성 회원·케틀벨 운동·F45 체육관 배경과 따뜻한 코칭 콘셉트를 참조해 새로운 모바일 사진을 생성함. 두 인물의 머리부터 신발, 팔과 손, 케틀벨이 모두 프레임 안에 들어오도록 여유 있는 전신 구도로 만들고 `assets/images/hero_frame_02_generated_430x600.jpg`로 별도 저장함. 기존 이미지와 캐러셀 연결은 변경하지 않음.

- **2026-08-30(히어로 1번 신규 세로 이미지 생성)**: 기존 사진을 단순 크롭하지 않고, 동일한 남성 트레이너·여성 회원과의 인사/악수·F45 체육관·파란 바닥·벽면 로고·밝은 자연광 콘셉트를 참조해 새로운 모바일 히어로 사진을 생성함. 최종본은 `assets/images/hero_frame_01_generated_430x600.jpg`로 별도 저장해 기존 이미지와 현재 캐러셀 연결은 변경하지 않음.

- **2026-08-30(히어로 1번 모바일 이미지 제작)**: `hero_frame_01.jpg` 원본은 보존하고, 웃는 남성 트레이너와 F45 로고가 보이도록 중앙 세로 크롭한 `hero_frame_01_430x600.jpg`를 별도 생성함. 생성형 보정 없이 원본 사진을 그대로 크롭·리사이즈함.

- **2026-08-30(히어로 5번째 이미지 교체)**: 처음에는 요청을 잘못 해석해 4번의 네 명 어깨동무 사진을 5번에도 중복 적용했으나 이를 바로잡음. 원본 `hero.mp4`의 5.5초 부근에서 9명이 F45 로고 앞에 모여 화이팅하는 장면을 다시 추출해 `hero_frame_05.jpg`로 교체하고, 브라우저 캐시 갱신용 버전 쿼리도 변경함.

- **2026-08-30(히어로 5장 자동 캐러셀 + 인디케이터)**:
  - 기존 APP 섹션의 pill형 인디케이터 디자인을 재사용해 히어로의 `체험권 신청하기` 버튼 바로 위에 5개 인디케이터를 추가함. APP 캐러셀과 상태가 섞이지 않도록 `.hero_dot` 전용 요소로 분리함.
  - 추출된 `hero_frame_01.jpg`~`hero_frame_05.jpg`를 모두 히어로에 쌓고 5초마다 0.9초 페이드로 자동 전환되게 구현함. 인디케이터 클릭으로 원하는 장면을 직접 선택할 수 있고, 이미지와 활성 인디케이터가 항상 함께 갱신됨.
  - 히어로 마우스 hover·키보드 focus 중에는 자동재생이 멈추고 벗어나면 다시 시작됨. `prefers-reduced-motion: reduce`에서는 자동재생을 시작하지 않고 CSS 전환 시간도 전역 모션 감소 규칙을 따름.

- **2026-08-30(히어로 꽉 채우기 + 영상 프레임 5장 추출)**:
  - 모바일 히어로 이미지의 `object-fit`을 `contain`에서 `cover`로 바꿔 600px 히어로 영역을 여백 없이 채우도록 수정함. 대표 이미지는 실제 `hero.mp4`의 0.5초 장면인 `assets/images/hero_frame_01.jpg`로 연결함.
  - 원본 영상 재생 시점 0.5·2.0·3.5·5.0·6.5초에서 서로 다른 프레임을 추출해 `hero_frame_01.jpg`부터 `hero_frame_05.jpg`까지 저장함. 모든 파일은 원본 해상도 1620×1080 JPEG이며 체크섬으로 중복이 아님을 확인함.

- **2026-08-30(PHASE·GUIDE 속도 재조정 + 히어로 정지 이미지 전환)**:
  - 모바일 PHASE가 너무 느리다는 피드백에 따라 직전 라운드의 requestAnimationFrame 보간·`scroll-behavior:smooth`·모바일 관성 추가값을 제거. PC와 동일하게 휠 delta를 scrollLeft에 즉시 반영하고 마우스 드래그/모바일 네이티브 터치 스크롤을 사용하는 구조로 통일함.
  - 모바일 GUIDE 페이드를 0.7s → 0.4s ease-out으로 단축하고, Observer threshold 0.2→0.1·하단 rootMargin -60%→-57%로 조정해 카드가 조금 더 일찍, 빠르게 나타나도록 함. 제자리 opacity 전환과 한 장씩 노출되는 방식은 유지.
  - 7.133초/1620×1080 hero.mp4에서 0.5·1.5·2.5·3.5·4.5·5.5·6.5초 후보를 브라우저 캔버스로 추출·비교하고, 코치의 표정과 F45 Training 로고가 가장 선명한 2.5초 프레임을 `assets/images/hero_mobile.jpg`(1620×1080 JPEG)로 저장. 히어로 `<video>`를 해당 정지 `<img class="hero_image">`로 완전히 교체해 더 이상 자동재생 영상이 로드되지 않음. 모바일은 이전 요청대로 전체 이미지가 잘리지 않는 `object-fit:contain`, PC는 기존 히어로 규격의 `cover`를 사용.

- **2026-08-30(모바일 스크롤·모션 후속 조정)**:
  - 모바일 히어로 영상은 `object-fit:cover` → `contain`으로 바꿔 원본 프레임이 잘리지 않게 했고, 모바일에서 기존 GSAP 히어로 확대 효과를 비활성화해 스크롤 중에도 다시 잘리지 않도록 함. 영상 비율 차이로 생기는 위아래 공간은 검정 배경으로 처리.
  - PHASE는 모바일 `scroll-snap`을 제거하고 `scroll-behavior:smooth`·iOS 관성 스크롤을 적용. 마우스 휠 경로는 목표 scrollLeft까지 requestAnimationFrame 보간(0.14)을 사용해 기존 즉시 이동의 뚝뚝 끊김을 완화함. 드래그/터치 조작 시 목표 위치를 현재 위치와 동기화함.
  - 모바일 지점 목록 높이를 291px(97px 항목 3개)로 고정해 교대·역삼·신사까지 보이고 이후 지점은 내부 스크롤로 확인하도록 변경. 우측에 6px lavender 스크롤바와 연한 트랙을 명시적으로 표시함.
  - 모바일 GUIDE 6카드에 IntersectionObserver 기반 순차 페이드인을 추가. 위치/transform은 바꾸지 않고 opacity만 0→1(0.7s)로 전환하며, 화면 상단 40% 감지 영역에서 한 장씩 노출. 초기 fade-out 플래시를 막기 위해 ready class를 다음 프레임에 추가하고, reduced-motion/Observer 미지원/비모바일 환경에서는 항상 바로 보이게 함.

- **2026-08-30(모바일 Figma node 769:1001 구현)**: 430px 모바일 원본을 `get_design_context`·스크린샷·메타데이터·변수 순서로 확인하고, 기존 PC 마크업과 기능을 유지한 채 `css/style.css`의 `@media (max-width:767px)`에서 전체 랜딩을 모바일 레이아웃으로 전환함.
  - 헤더 64px(텍스트 로고+메뉴 버튼), 히어로 600px, WHY F45 KOREA 세로형 280px 카드 5개, 106px 마퀴, 230×394px PHASE 가로 스크롤 카드, 검색→도시/주변매장→지도→지점목록 순서의 Locator, 숫자를 숨긴 6개 컴팩트 가이드 카드, 번호→180×360px 폰→문구/스토어 버튼→dot 순서의 앱 섹션, 296px 푸터를 Figma 구조와 실측값에 맞춤.
  - 히어로에는 Figma상 별도 이미지가 없으므로 사용자 요청에 따라 기존 PC `hero.mp4`를 `object-fit:cover`로 세로 크롭해 사용하고, poster/reduced-motion 폴백은 그대로 유지함.
  - DOM 순서가 PC와 다른 지도/지점목록 및 앱 안내 요소는 새 마크업을 복제하지 않고 CSS `order`/`display:contents`로 재배치. WHY O/E 행도 모바일에서 사진이 먼저 오도록 order만 조정함.
  - 모바일 검색 버튼은 디자인에 없어 숨기되 Enter/검색 키로 필터링되도록 `js/app.js`에 명시적 keydown 처리를 추가하고, 브라우저 캐시로 구버전 JS가 남지 않도록 app.js에 버전 쿼리를 부여함.
  - 헤더/푸터 모바일 텍스트 로고용 span만 최소 추가했으며, PC SVG 로고는 768px 이상에서 기존 그대로 유지함.

- `index.html` 전면 재작성: 헤더, 히어로(체험권 신청 CTA 포함), WHY F45 KOREA(intro_k/o/r/e/a.jpg 5장, K-O-R-E-A 순서), 마퀴(확인된 문구 "누구나 시작할 수 있어요 · F45 KOREA"), 이번 PHASE 7카드(phase_pipline·lonestar·docklands·fusion·thenines·west·angrybird.jpg), 지점 찾기(도시 드롭다운 + 검색 + 주변 매장 찾기 + 지점 6곳 리스트 + map.jpg), 입문자 케어 가이드 01~06(보라색 알파 단계), 앱 설치(phone01~06.png 캐러셀 + 인디케이터), 푸터(대표자·사업자등록번호·소셜 5종·메뉴·저작권)
- `css/style.css` 전면 재작성: `:root` 토큰을 design-analysis.md 확인값(teal #008ba3, lavender #5b5ceb, bg #f8f9fa, text #0a0913 등)으로 교체, `@font-face`로 Pretendard(woff2)·Montserrat(ttf) 로컬 폰트 연결, 버튼/카드/라운드 값을 확인된 스펙으로 반영, hover/focus-visible/active/disabled 상태 신규 추가, `prefers-reduced-motion` 대응 추가
- `js/app.js` 신규 작성: 도시 드롭다운(열기/닫기/선택/외부클릭·Escape 닫기), 지점 검색+도시 필터 결합 필터링, 주변 매장 찾기(필터 초기화), 트라이얼 앱 6단계 캐러셀(좌우 화살표+인디케이터 dot 클릭) — ~~검색어 없을 때 검색 버튼 disabled~~는 2026-08-07에 제거됨(아래 참고)
- 모든 클래스/아이디를 snake_case로 통일(기존 kebab-case 잔재 정리)
- **2026-07-30**: 로고 파일 교체(`assets/icons/logo_1.svg` 삭제 → `logo.svg` 신규) 대응, 헤더/푸터 `index.html`의 로고 참조를 `logo.svg`로 수정
- **2026-07-30**: Figma MCP 재인증 후 헤더(`node 632:378`)·히어로(`node 632:394`) 원본을 실측 대조해 아래 불일치를 수정
  - 헤더/푸터 로고 크기가 실측(75×57px)보다 작게(헤더 32px, 푸터 28px 높이) 렌더링되던 문제 수정 → `css/style.css`의 `.logo`, `.logo img`, `.footer_logo img`를 75×57px + 10px 패딩(헤더)으로 교체
  - 히어로 배경을 3분할 이미지 콜라주(phase_pipline/fusion/thenines.jpg)에서 실제 디자인대로 풀블리드 단일 배경으로 교체하고, 새로 업로드된 `assets/imges/hero.mp4`를 배경 영상으로 반영(`autoplay muted loop playsinline`, `prefers-reduced-motion: reduce`에서 숨김 처리)
  - 디자인에 없던 원형 "F45/KOREA" 배지(`hero_badge`)를 제거(Figma 원본에 해당 요소 없음 확인)
  - 히어로 타이틀 폰트 크기를 실측값 88px로, 텍스트/버튼 정렬을 좌측→하단 중앙으로 수정(Figma 원본이 중앙 정렬)
- **2026-07-30**: 히어로 영상 코덱 문제 및 접근성 폴백 보완
  - 업로드된 `hero.mp4`가 확장자만 mp4이고 실제로는 QuickTime 컨테이너(`ftyp qt`)라 대부분 브라우저에서 재생되지 않던 문제를 발견 → macOS `avconvert`(PresetPassthrough)로 코덱 재인코딩 없이 표준 MP4 컨테이너(`ftyp mp42/isom`)로 리먹싱해 교체(코덱 H.264/AAC, 해상도 1620×1080 그대로 유지)
  - `hero.mp4`에서 정지 프레임을 추출해 `assets/imges/hero_poster.jpg` 신규 생성 → `<video poster>`로 연결하고, `prefers-reduced-motion: reduce`일 때 `.hero` 배경 이미지로도 사용(이전엔 빈 그라데이션만 보여 동작 줄이기 사용자에게 아무 이미지도 안 보이던 문제 개선)
  - CSS 버그 수정: reduced-motion용 `.hero` 배경 규칙을 앞쪽 미디어쿼리 블록에 넣었더니 뒤에 나오는 `.hero { background: ... }` 규칙에 밀려 무시되던 문제 → `.hero` 규칙 뒤에 별도 미디어쿼리로 재배치해 해결
- **2026-07-30**: WHY F45 KOREA 섹션(`node 632:400`)을 Figma와 실측 대조해 전면 수정
  - 220px 균등 그리드 + K/O/R/E/A 대형 글자 배지 구조(디자인에 없던 요소)를 제거하고, 400×400px 대형 사진 5장이 좌우 지그재그로 배치되고 각 사진 옆에 제목+2줄 설명이 나란히 놓이는 실제 구조로 교체(`korea_row_k/o/r/e/a`)
  - 각 카드의 2줄 설명 문구(예: "과학 기반 기능성 트레이닝 / 근육과 심혈관 건강을 동시에 개선" 등 5세트)를 Figma에서 새로 확인해 추가 — 기존엔 이 문구들이 HTML에 전혀 없었음
  - 사진/텍스트 카드별 회전(K:2deg, O:-2deg, R:-2deg, E:2deg, A:2deg), 행간 간격(4px), 섹션 배경 대각선 그라데이션, 타이틀 크기(영문 30px/한글 20px, 기존엔 반대로 되어 있었음)를 실측값으로 수정

- **2026-08-06**: 지점 찾기(`#locator`, node 632:570)·앱 설치(`#app`/trial, node 632:705) 두 섹션을 Figma 재실측해 전면 재구현
  - 지점 찾기:
    - 컨테이너를 공용 `.wrap`(1440 max-width + 좌우 40px 패딩)에서 전용 `.locator_inner`(1440 max-width, 패딩 없음)로 교체 — 기존 `.wrap`을 쓰면 콘텐츠 실폭(500+940=1440px)이 가용폭(1360px)을 넘어 가로 스크롤이 생기는 문제를 확인해 수정
    - 컨트롤 영역 마크업을 Figma 중첩 구조(도시 드롭다운 → `.locator_search_wrap`(gap 28) → 검색폼(gap 8)+주변매장찾기)로 재구성 — 기존엔 드롭다운과 검색창 사이 gap이 28px이 아닌 8px로 잘못 그룹핑돼 있었음
    - "검색" 버튼을 실측대로 lavender-5% 배경 + lavender 테두리 + 검정 텍스트로 수정(기존엔 lavender 단색 배경 + 흰 텍스트로 디자인과 다르게 구현돼 있었음)
    - 검색 인풋 폰트를 Montserrat 우선(Pretendard 폴백)으로 수정(Figma가 Montserrat 지정)
    - "상세페이지 보기" 버튼 배경을 `#fff`에서 투명으로 수정(리스트 배경 `--color-bg`와 동일 톤이어야 함)
    - 지점 리스트(`.branch_list`)를 지도 이미지와 동일한 627px 높이로 고정하고 `overflow-y:auto` 스크롤 적용 — Figma의 `map_txt`/`map_img`가 둘 다 h-627로 명시돼 있고 6개 항목 실제 합계(약 774px)가 이를 초과해 스크롤 영역임을 확인, 스크롤바는 얇은 lavender 색상으로 스타일링(Figma의 우측 세로 라인 요소를 네이티브 스크롤바 스타일로 재해석)
  - 앱 설치(`#app`):
    - 기존 구현("F45 Korea (new) 만나보기" 타이틀 + 1~6단계 텍스트 목록 + 체험권 신청하기 버튼 + 겹친 폰 이미지 2장)이 Figma 원본과 완전히 달라 전면 교체
    - Figma 실측대로 단일 폰 목업(282×570, `phone01~06.png` — 이미 정확한 282×570px 합성 목업으로 준비돼 있던 로컬 에셋 확인 후 그대로 사용) + "01/06" 형태 페이지 번호 + "F45 - Korea (new) 모바일 앱 설치 / 설치는 1분이면 충분해요" 문구 + App Store/Play Store 버튼(`apple.png`/`playstore.png`, 실측 크기와 로컬 에셋 크기 일치 확인) + 좌우 화살표(`left.png`/`right.png`)·인디케이터 dot(활성 32×16 teal pill, 비활성 16×16 `#ddd`)로 재구성
    - Figma 디자인에 없던 "체험권 신청하기" CTA 버튼은 제거(PRD 9장 사용자 흐름에서도 체험권 CTA는 히어로 영역 전용으로 명시돼 있어 중복이 아니라 원래 이 섹션에 없는 요소였음을 확인)
    - `js/app.js`에 `initTrialCarousel()` 신규 작성: 좌우 화살표·인디케이터 dot 클릭 시 phone 이미지·"01/06" 숫자·활성 dot을 함께 갱신(6장 순환)

- **2026-08-06(되돌림)**: 사용자가 전달한 GSAP(seamless-loop cards, ScrollTrigger pin + Draggable) 스크립트를 앱 설치(`#app`) 섹션에 한 차례 적용했으나, 사용자가 "하나도 안 맞다"며 되돌려 달라고 요청해 전체 되돌림 — `index.html`의 `.gallery`/`.cards`/`.drag-proxy`/`.prev`/`.next` 마크업, GSAP CDN 스크립트 3종, `js/trial_gallery.js`(신규 파일), 관련 CSS(`.trial_phone_frame`/`.cards`/`.drag-proxy`)를 모두 제거하고 바로 위 항목(단일 폰 이미지 + dot 인디케이터 + `initTrialCarousel()`) 상태로 복원함.
- **2026-08-06(라이브러리 없는 CSS 전환 애니메이션으로 재시도)**: 되돌린 뒤 사용자와 함께 방향을 다시 정함(AskUserQuestion) — 전환 방식은 "스케일+페이드", 자동재생은 "정말 천천히 넘어가고 마우스 호버 시 일시정지"로 확정. GSAP 없이 순수 CSS transition + 바닐라 JS `setInterval`로 구현(PRD 16장 "CSS·기본 JS로 가능하면 라이브러리 없이" 원칙에 맞음).
  - `index.html`: `.trial_phone`(282×570) 안에 폰 이미지 6장을 `<img class="trial_phone_slide">`로 겹쳐 쌓는 구조로 변경(기존 단일 `<img>` src 교체 방식은 크로스페이드가 불가능해 구조 변경이 필요했음). `trial_right`/화살표/dot 마크업은 그대로 유지.
  - `css/style.css`: `.trial_phone_slide`에 `opacity 0→1` + `transform scale(0.94)→scale(1)`을 `transition: 0.9s ease`로 적용(스케일+페이드), `is_active`에 `z-index:2`를 줘서 들어오는 화면이 나가는 화면 위로 겹치며 자연스럽게 전환되게 함. `.trial_dot`에도 width/배경색 전환(0.3s) 추가.
  - `js/app.js`의 `initTrialCarousel()`을 재작성: 화살표/dot 클릭은 기존과 동일하게 동작하되, `renderSlide()`가 이제 6개 슬라이드의 `is_active` 클래스를 토글. `setInterval`로 7초마다 자동 다음 화면 전환 추가, `.trial_inner`에 `mouseenter`/`mouseleave`로 자동재생 정지/재개, `prefers-reduced-motion: reduce` 사용자는 자동재생을 아예 시작하지 않도록 처리(수동 조작은 계속 가능). 화살표/dot로 수동 이동하면 자동재생 타이머를 리셋해 바로 다음 자동 전환이 이어지지 않게 함.
- **2026-08-06(우측 안내 문구를 슬라이드별로 다르게 수정)**: 사용자가 "6개 오른쪽 텍스트도 다 다르다"고 지적 — 그동안 슬라이드가 바뀌어도 우측 제목/설명("F45 - Korea (new) 모바일 앱 설치" / "설치는 1분이면 충분해요")이 01번 문구로 고정돼 있던 문제를 수정.
  - Figma의 `trial` 컴포넌트 정의(`get_metadata` node 695:313)를 다시 확인했으나 01단계 문구만 고정 노출되고 02~06단계용 별도 텍스트 variant는 없었음 — 대신 `phone02~06.png`를 직접 열어 화면 내용(도시 검색 → 로그인/회원가입 → 멤버십 탭이 눌린 시간표 화면 → 크레딧 구매 화면 → Duo 클래스 상세+예약하기 버튼)을 확인했더니 PRD.md 7·9장에 이미 적혀 있던 "F45 - Korea (new) 모바일 앱 설치 / 수강을 원하는 지점 검색 / 회원가입 또는 로그인 / 하단에 멤버십 탭 클릭 / 상단 크레딧 구매에서 체험권 구매 / 예약하기 버튼 클릭" 6단계 문구와 정확히 일치함을 확인 → 이 PRD 문구를 슬라이드별 제목으로 사용.
  - 2번째 줄(설명, "설치는 1분이면 충분해요")은 Figma·PRD 어디에도 01단계 외 다른 5단계용 문구가 없어 임의로 지어내지 않고 02~06단계는 빈 문자열로 둠(단, `<p>` 요소 자체는 유지해 빈 줄도 line-height만큼 공간을 차지하게 해서 슬라이드가 바뀔 때 버튼 위치가 위아래로 흔들리지 않게 함) — 02~06단계 설명 문구가 별도로 있다면 추가 확인 필요.
  - `index.html`의 `trial_txt_title`/`trial_txt_desc`에 id 추가, `js/app.js`에 `stepTexts` 배열(6개 title/desc 쌍)을 추가해 `renderSlide()`가 슬라이드 전환 시 이 두 문단의 textContent도 함께 갱신하도록 수정.
- **2026-08-06(Figma 컴포넌트 라이브러리로 6단계 문구·버튼 확정 + 우측 슬라이드 애니메이션 추가)**: 사용자가 "Figma 컴포넌트에서 확인하라"며 [node 704:426](https://www.figma.com/design/xYrHj7h7ULDihhqhQmSrX2/%EC%86%A1%EB%AA%85%ED%9D%AC?node-id=704-426&m=dev)(컴포넌트 라이브러리 페이지)를 직접 지정 — 이전엔 라이브 페이지 인스턴스(01단계 상태)만 봐서 02~06단계 문구를 못 찾았는데, 이 컴포넌트 라이브러리 페이지에는 `trial` step=1~6 variant가 전부 개별 정의돼 있어 6단계 모두의 정확한 문구를 확인함.
  - 확정된 6단계 문구(제목/설명, PRD 기반 추정이었던 이전 값과 제목은 일치·설명은 새로 확인):
    1. F45 - Korea (new) 모바일 앱 설치 / 설치는 1분이면 충분해요
    2. 수강을 원하는 지점 검색 / 우리 동네 F45가 바로 나와요
    3. 회원가입 또는 로그인 / 쉽고 간편한 로그인과 회원가입으로 빠르게 시작해요
    4. 하단에 멤버십 탭 클릭 / 하단 탭 하나면 충분해요
    5. 상단 크레딧 구매에서 체험권 구매 / 크레딧 결제까지 순식간에 끝나요
    6. 예약하기 버튼 클릭 / 이제 F45에서 만나요
  - **새로 발견한 사실**: 버튼도 단계별로 다름 — step=1만 App/Play Store 버튼(`pay_button`), step=6만 "체험권 신청하기" CTA(`cta`, bg `#008ba3` w-300 h-64), step=2~5는 버튼이 아예 없음. 이전 라운드에 "Figma에 CTA가 없다"고 판단해 체험권 신청 버튼을 완전히 제거했던 건 01단계만 보고 내린 결론이라 부정확했음 — `.trial_cta`를 다시 추가하되 06단계에서만 보이도록 수정.
  - `index.html`: `trial_store_buttons`/신규 `trial_cta` 버튼에 id 부여, `trial_cta`는 기본 `is_hidden`으로 시작(01단계 기본 노출은 store 버튼).
  - `css/style.css`: `.trial_store_buttons.is_hidden`/`.trial_cta.is_hidden { display:none }`, `.trial_cta` 스타일 재추가.
  - **오른쪽 슬라이드 애니메이션**: "왼쪽 폰뿐 아니라 오른쪽도 자연스럽게 슬라이드되게" 요청에 따라 `.trial_right`(번호+제목+설명+버튼 전체)에 슬라이드+페이드 트랜지션 추가. 폰 이미지는 두 장을 겹쳐두고 CSS만으로 크로스페이드하지만, 오른쪽은 단계마다 문구·버튼 "내용" 자체가 바뀌어야 해서 같은 방식을 못 씀 — 대신 `js/app.js`에서 (1) 현재 내용을 슬라이드 아웃(다음=왼쪽으로, 이전=오른쪽으로, 280ms) → (2) 텍스트/버튼 내용 교체 → (3) 반대편에서 트랜지션 없이 대기(`is_sliding_in_*`) → (4) 다음 프레임에 그 클래스를 떼서 제자리로 슬라이드 인, 순서로 타이밍을 맞춤. `prefers-reduced-motion` 사용자는 이 애니메이션을 건너뛰고 즉시 갱신.
- **2026-08-06(왼쪽·오른쪽 전환 타이밍 동기화 + 오른쪽 세로 위치를 Figma 실측대로 수정)**: 사용자가 "텍스트랑 왼쪽 이미지가 넘어가는 타이밍이 살짝 어긋나고 오른쪽 위치도 디자인이랑 다르다"고 지적해 두 가지를 수정.
  - **세로 위치 버그**: Figma의 `trial_con`(번호+문구+버튼을 담는 박스)이 모든 단계에서 `padding-bottom: 171px`을 갖고 있고, 이 171px을 포함한 박스가 phone(570px)과 함께 세로 중앙 정렬되는 구조였음(컴포넌트 라이브러리 재확인). 이전 구현은 이 171px 패딩을 빼먹고 콘텐츠만 순수 중앙 정렬해서, 버튼이 있는 01·06단계는 Figma보다 훨씬 아래(콘텐츠 높이 392px 기준 중앙, 위쪽 여백 약 89px)에, 버튼 없는 02~05단계는 더더욱 크게 어긋나 있었음 → `css/style.css`의 `.trial_right`에 `padding-bottom: 171px` 추가로 수정. 이제 01·06단계는 거의 맨 위(약 3.5px 여백)에, 02~05단계는 콘텐츠 높이에 따라 자연스럽게 그보다 아래(약 63~79px 여백)에 위치함 — Figma와 동일한 계산식.
  - **타이밍 어긋남 버그 1(시작 시점)**: 기존엔 왼쪽 폰의 `is_active` 토글이 `applyStepContent()` 안에 있었고, 이 함수는 오른쪽 텍스트가 다 빠져나간 뒤(280ms 후)에야 호출됐음 — 즉 폰 크로스페이드가 텍스트보다 280ms 늦게 시작하고 있었음. `applyPhoneAndDots()`(폰·dot, 자체 트랜지션 있어 즉시 실행 가능)와 `applyTextContent()`(문구·버튼 내용, 화면 밖으로 빠진 뒤에만 교체 가능)로 함수를 분리해 폰·dot 갱신을 텍스트가 빠지기 시작하는 것과 같은 시점(t=0)에 실행하도록 수정.
  - **타이밍 어긋남 버그 2(전체 모양)**: t=0에 함께 시작하도록 고쳐도, 텍스트는 "빠짐(280ms)→내용 교체→들어옴(나머지)" 구조라 처음 절반은 화면에서 사라져 있고 후반부에만 보이는데, 폰은 900ms 내내 연속으로 크로스페이드돼서 "같이 움직이는" 느낌이 덜했음(중간 프레임 스크린샷으로 확인: 폰은 거의 다 나타났는데 텍스트는 아직 반투명). → 나가는 시간을 150ms로 짧게, 들어오는 시간을 750ms로 길게 재배분(총 900ms로 폰과 동일하되 "들어오는" 구간이 폰의 크로스페이드 대부분과 겹치도록). `css/style.css`의 `.is_sliding_out_*`에 0.15s 트랜지션을 별도로 지정(기존 `.trial_right` 기본값은 0.75s로 "들어올 때" 용도), `js/app.js`의 `textOutMs`도 150으로 수정.
- **2026-08-06(위 세로 위치 수정을 재수정 — "2~5단계도 1·6단계처럼 상단으로")**: 사용자가 "02~05단계도 01·06단계처럼 상단에 붙여달라"고 재지시 — 바로 위 항목의 "pb-171 + 중앙 정렬" 방식은 콘텐츠 높이가 짧은 02~05단계가 여전히 아래로 처지는 근본적인 한계가 있었음(중앙 정렬인 이상 짧은 콘텐츠는 항상 더 아래로 감). Figma 구조를 다시 보니 실제로는 "중앙 정렬"이 아니라 "trial_right가 phone과 같은 570px로 늘어난 뒤, 그 안에서 콘텐츠가 항상 위쪽부터 채워지는" 구조였음(`h-full`/`self-stretch`+`items-start` 조합) — 즉 pb-171은 위치 계산에 실질적 영향이 없는 값이었고, 애초에 "늘림+상단 정렬"이 맞는 해석이었음.
  - `css/style.css`: `.trial_row`를 `align-items: center` → `align-items: stretch`로, `.trial_right`는 `padding-bottom: 171px` 제거하고 `height: 100%` + `overflow: hidden`으로 교체(phone과 같은 570px로 늘어나고 내부 콘텐츠는 항상 위쪽부터 채움 — `justify-content: flex-start` 명시).
  - 스크린샷으로 04단계("하단에 멤버십 탭 클릭", 버튼 없음)가 01단계와 거의 동일한 y 위치에서 시작하는 것을 확인함 — 이전 pb-171 방식(약 78px 아래)보다 확연히 위로 붙음.
- **2026-08-07(지점 찾기 도시 드롭다운 — Figma `city_button`/`city_all` 컴포넌트 실측 반영)**: 사용자가 지정한 [node 755:640](https://www.figma.com/design/xYrHj7h7ULDihhqhQmSrX2/%EC%86%A1%EB%AA%85%ED%9D%AC?node-id=755-640&m=dev)(city_button, city/city_close 두 variant)·[node 755:679](https://www.figma.com/design/xYrHj7h7ULDihhqhQmSrX2/%EC%86%A1%EB%AA%85%ED%9D%AC?node-id=755-679&m=dev)(city_all, 목록)를 확인해 두 가지를 정확히 구현.
  - **버튼 모양이 열림/닫힘 상태에 따라 달라짐**: 닫힘("city_close" variant) = 완전히 둥근 알약 모양 + 아래쪽 화살표(`down.png`), 열림("city" variant) = 위쪽만 둥글고 아래쪽은 각짐 + 위쪽 화살표(`up.png`) — 열렸을 때 버튼이 아래 목록과 이어 붙어 하나의 알약 모양을 이룸. 기존엔 항상 완전히 둥근 모양 + 항상 아래쪽 화살표만 쓰고 있었음 → `css/style.css`에 `.city_dropdown.is_open .city_dropdown_toggle` 규칙 추가, `js/app.js`의 `openDropdown()`/`closeDropdown()`에서 `#city_dropdown_icon`의 `src`를 `up.png`/`down.png`로 교체.
  - **목록 스타일**: 항목 간 간격 없이 붙어 있고(기존엔 8px 컨테이너 패딩 + 4px 항목 간격 있었음), 각 항목은 20px Pretendard Medium(기존 14px는 오타 수준의 불일치였음), 선택된 항목만 lavender 배경/텍스트(굵기는 동일 — 기존엔 `font-weight:700`으로 굵게 처리하던 게 실측과 다름), 마지막 항목(남부)만 아래쪽 모서리가 둥긂(컨테이너 전체가 아니라). → `.city_dropdown_list`의 컨테이너 패딩·gap·둥근 모서리 제거, `.city_dropdown_item`에 `padding: 12px 91px 12px 20px`, `font-size: 20px` 적용, `.city_dropdown_item:last-child`에만 하단 모서리를 둥글림, `.is_selected`에서 `font-weight` 제거.
  - **버튼·목록 너비를 정확히 146px로 고정하면 실제로 깨짐**: Figma 값(146px) 그대로 두면 "도시 선택" 텍스트가 Pretendard SemiBold 20px 기준 브라우저에서 두 줄로 줄바꿈되는 것을 스크린샷으로 실제 확인(폰트 렌더링 차이로 추정) → 버튼은 `width` 고정을 풀고 `white-space: nowrap`으로 내용에 맞게 자동으로 늘어나게 하고, 목록은 `width: 146px` 대신 `width: 100%`로 바꿔 절대위치 기준 부모(`.city_dropdown`, 버튼의 실제 렌더링 너비를 그대로 물려받음)를 따라가게 함 — 고정 픽셀값 대신 "항상 버튼과 같은 너비"라는 관계 자체를 CSS로 표현해 폰트가 달라져도 안 깨지도록 구현.
  - **도시 선택 버튼 라벨 문구도 Figma에 맞춰 되돌림**: 기존엔 도시를 선택하면 라벨이 "도시 선택 · 서울"처럼 바뀌었는데, Figma 버튼은 선택 상태와 무관하게 "도시 선택" 고정 문구임 — `js/app.js`에서 라벨 텍스트를 동적으로 바꾸던 코드 2곳(항목 클릭 시, 주변 매장 찾기 클릭 시) 제거. 선택된 도시는 목록을 열었을 때 강조 항목으로 확인 가능.
  - 스크린샷으로 열림 상태(우연히 캡처된 프레임)에서 버튼과 목록의 좌우 테두리가 정확히 일치하고, "전체"만 강조되고 나머지는 흰 배경, 남부만 아래 모서리가 둥근 것을 확인함.
- **2026-08-07(SEO 메타 태그 추가)**: 사용자가 전달한 SEO 메타(`description`/`keywords`/`author`/`robots`/`canonical`/`theme-color`)를 `<head>`에 추가. 이미 있던 `viewport`·`title`은 중복 추가하지 않음. **사용자가 "파비콘 코드"라고 표현했지만 실제로 전달된 코드에는 `<link rel="icon">` 등 파비콘 태그가 없었음** — 존재하지 않는 파일을 임의로 만들지 않기 위해 파비콘 링크는 추가하지 않았고, 실제 파비콘 코드/이미지가 있으면 별도로 요청 필요.
- **2026-08-07(파비콘 세트 적용)**: 사용자가 실제 파비콘 `<link>` 태그(favicon.svg/ico/16/32, apple-touch-icon, icon-192/512, site.webmanifest)를 다시 전달 — 파일시스템을 확인해보니 `favicon-og-set/favicon/`에 이미 해당 파일 9개(요청된 7개 + `icon-512-maskable.png`)가 실제로 업로드돼 있었음.
  - 사용자가 전달한 코드가 `/favicon/...`(루트 절대경로)를 참조했는데, 이 프로젝트는 GitHub Pages에 `https://myeonghee0187-sys.github.io/f45_korea/`(서브패스)로 배포됨(이미 `<link rel="canonical">`에 있는 값) — 절대경로 그대로 쓰면 로컬(Live Server, 루트가 곧 사이트 루트)에서는 우연히 동작하지만 실제 배포 시 `f45_korea/` 없이 도메인 루트에서 파비콘을 찾아 깨짐. 기존 프로젝트 관례(`./assets/...`, `./css/...`)와 동일하게 전부 상대경로(`./favicon/...`)로 적용.
  - 폴더 구조를 `favicon-og-set/favicon/*` → 프로젝트 루트 `favicon/*`로 이동(사용자가 코드에서 의도한 `/favicon/` 경로와 일치시킴, 불필요한 중첩 제거). `favicon-og-set/`에는 참고용 원본 `meta-tags.html`(OG·트위터 카드 태그 포함, 이번엔 요청 범위 아니라 미적용)만 남음.
  - `favicon/site.webmanifest` 내부의 아이콘 경로도 `/favicon/icon-192.png` 등 루트 절대경로였던 것을 `icon-192.png`(매니페스트 파일 기준 상대경로, 스펙상 매니페스트 자신의 위치 기준으로 풀림)로 수정 — 위와 같은 서브패스 배포 이슈를 매니페스트 안에서도 동일하게 수정.
  - 이번에 다시 전달된 코드의 `description`/`keywords`/`author` 값이 지난번과 달라져 있어(예: description "바쁜 현대 사회 속 45분 안에 기능성 트레이닝을 경험!"로 변경) 같이 최신값으로 갱신함.
  - 파일 존재 여부를 `favicon/` 아래 8개 파일 전부 `ls`로 재확인, Chrome에서 새로고침 후 콘솔 에러 없이 로드되는 것을 스크린샷으로 확인(단, 브라우저 탭 아이콘 자체가 스크린샷 해상도에서 육안으로 뚜렷이 보이지는 않아 탭 아이콘 렌더링 자체까지 픽셀 단위로 확인하지는 못함 — 파일 경로·MIME·사이즈 속성은 표준 파비콘 세트 관례와 일치).
- **2026-08-07(도시 드롭다운 목록 항목 hover 색상 + 기본 강조 제거 + 패딩 정리)**: 사용자가 "선택했을 때만 컬러가 바뀌는데 호버해도 바뀌었으면 좋겠다"고 요청 → `.city_dropdown_item:hover`을 `.is_selected`와 같은 색(라벤더 배경 `--color-lavender-selected-bg` + 라벤더 텍스트 `--color-lavender`)으로 변경.
  - 곧이어 "제일 위 칸(전체)에 컬러가 계속 고정돼 있다"는 후속 지적 → `index.html`에서 "전체" 항목에 기본으로 붙어 있던 `is_selected` 클래스를 제거(Figma의 `city_all` 목록은 "전체"가 항상 재현될 필요가 있는 selected 상태가 아니라, 강조 스타일이 어떻게 생겼는지 보여주는 예시 스크린샷 1장이었을 뿐 — `city_num`이 재사용 컴포넌트/variant가 아닌 걸 `get_metadata`로 재확인함). `getSelectedCity()`가 미선택 시 `"전체"`로 폴백하는 로직은 그대로라 지점 목록 초기 표시(전체 노출)에는 영향 없음.
  - **디테일 재점검**: 목록 항목 패딩이 `12px 91px 12px 20px`(Figma의 146px 고정폭 기준 계산값)였는데, 지난 라운드에 버튼·목록 폭을 `width:100%`(버튼 실제 렌더링 폭을 따라가도록)로 바꾼 뒤로는 91px이 더 이상 의미 있는 값이 아니게 돼(폭이 넓어질수록 텍스트 뒤 여백만 커짐) → 좌우 대칭 `12px 20px`로 단순화. 목록 항목이 컴포넌트/variant가 아니라 hover/selected 등 Figma가 정의한 추가 상태가 없음을 재확인해, 이 이상의 "디테일"은 이미 확보된 값(그림자 `--shadow-dropdown`, 폰트 20px Medium, 마지막 항목만 하단 라운드)이 전부임을 검증함.
  - 검증: 클릭 시뮬레이션이 안 돼(Apple Events 접근성 차단) `index.html`의 `.city_dropdown`에 `is_open`을 임시로 붙여 강제로 펼친 상태를 스크린샷으로 확인 후 원복. 이 과정에서 "전체"만 라벤더로 보인 것은 실제 OS 마우스 커서가 그 위치에 남아 있어 `:hover`가 실제로 걸린 것이었고(다른 항목들은 흰 배경으로 대조됨), 코드에 `is_selected`가 없음을 `grep`으로도 재확인해 "고정 강조" 버그가 실제로 제거됐음을 확인함.
- **2026-08-07(마지막 항목 하단 라운드가 실제로는 안 보이던 문제 수정)**: 사용자가 "남부칸 하단에 border-radius 20이 들어가있는데 화면에선 직사각형으로 보인다"고 지적 — `sips`로 스크린샷을 크롭해 확대해보니 실제로는 `.city_dropdown_item:last-child`의 radius 자체는 적용되고 있었음(호버 시 라벤더 배경으로는 둥근 모서리가 보임)이지만, 두 가지 문제가 겹쳐 기본 상태에서는 안 보였음: (1) 항목 배경(`--color-bg` #f8f9fa)과 목록 배경(#fff)의 색 차이가 거의 없어 둥근 모서리 부분이 육안으로 구분이 안 됨, (2) `box-shadow`가 각진 사각형인 `.city_dropdown_list`에 걸려 있어서 그림자 윤곽 자체가 여전히 사각형으로 보임.
  - `css/style.css`: `border-radius`(아래쪽만)와 `overflow: hidden`을 항목이 아니라 `.city_dropdown_list` 자체에 걸어서, 그림자가 실제 둥근 모양을 따라가고 내부 항목도 그 모양대로 확실히 잘리도록 수정. 항목별 `:last-child` radius 규칙은 이제 컨테이너가 클리핑을 담당하므로 제거.
  - 검증: 다시 `is_open`을 임시로 붙여 펼친 상태를 스크린샷 → `sips -c`로 "남부" 항목 하단 좌우 모서리를 확대 크롭 → 마우스가 그 항목에 없는 기본 상태에서도 둥근 모서리가 명확히 보이는 것을 확인 후 임시 클래스 원복.
- **2026-08-07(드롭다운 열림/닫힘 애니메이션 추가)**: 사용자가 "드롭다운될 때 너무 띡띡거린다"고 지적 — 기존엔 `.city_dropdown.is_open .city_dropdown_list { display:flex }`로 `display:none`↔`flex`를 즉시 토글해서 트랜지션이 걸릴 수 없는 구조였음(display는 애니메이션 불가 속성).
  - `css/style.css`의 `.city_dropdown_list`를 `display:none` 대신 항상 `display:flex`로 두고, `opacity`(0→1)+`transform: translateY(-8px→0)`으로 부드럽게 열리고 닫히게 함(0.2s ease). 닫힘 상태에서 화면에 안 보이면서 클릭도 안 되도록 `visibility:hidden`+`pointer-events:none`을 같이 쓰되, `visibility`는 닫힐 때만 트랜지션이 끝난 뒤(0.2s 딜레이) 전환되도록 해서 열릴 땐 바로 보이고 닫힐 땐 서서히 사라진 뒤에 완전히 숨겨지게 함. 버튼의 모서리(각짐↔둥긂) 전환에도 `transition: border-radius 0.2s`를 추가해 목록이 열리는 속도와 맞춤.
  - `js/app.js`는 `.is_open` 클래스만 토글하는 기존 구조 그대로라 변경 불필요(코드 확인).
  - 검증: 실제 클릭으로 열고 닫는 트랜지션 "재생 중" 화면은 Apple Events로 클릭을 못 해 캡처하지 못함(전환 애니메이션이 순간적이라 트리거 자체가 안 되면 중간 프레임을 볼 방법이 없음) — 코드 리뷰로 트랜지션 속성이 올바르게 걸린 것과, 닫힘(기본) 상태가 여전히 정상 렌더링되는 것(목록이 새지 않음)만 스크린샷으로 확인함. 실제로 부드럽게 느껴지는지는 사용자가 브라우저에서 직접 열어봐야 확인 가능.
- **2026-08-22(마퀴 문구 구분자 추가 → 여러 차례 조정)**: 사용자가 "마퀴 문구별 구분자가 없어서 가독성이 떨어진다"며 불릿(•) 추가를 요청, 다른 방법(세로바, 여백만 넓히기, "F45 KOREA" 브랜드 컬러 강조, 로고 마크, 슬래시/별표)도 코드 수정 없이 제안해달라고 해서 텍스트로만 제안함.
  - 1차: `css/style.css`의 `.marquee_track span`에서 `padding:0 20px`(문구 간 여백만)를 제거하고 `::after{content:"•"}` + `margin:0 20px`로 교체(모든 span에 적용 — 텍스트가 두 벌 이어붙어 무한 스크롤되는 구조라 마지막 span도 예외 두면 이어지는 지점에서 간격이 끊김).
  - 사용자가 "1번(세로바 |)+3번(F45 KOREA 브랜드 컬러) 같이 적용" 요청 → `index.html`의 "F45 KOREA" span 6곳에 `class="marquee_brand"` 추가, CSS에 `.marquee_brand{color:var(--color-teal);font-weight:700}` 추가하고 구분자를 `content:"|"`로 교체 → 격리된 새 Chrome 창(포트 8935/8936, 프로젝트와 무관한 사용자의 다른 탭들을 건드리지 않기 위해)에서 확대 캡처로 둘 다 정상 적용된 것을 확인.
  - 사용자가 "그냥 불릿으로"(구분자만 원복), 이어서 "브랜드 컬러 강조도 없애줘"라고 순차 요청 → 구분자를 다시 `content:"•"`로, `.marquee_brand` 규칙과 `index.html`의 `class="marquee_brand"` 6곳 전부 제거해 최종적으로 기존 문구 스타일 그대로에 불릿 구분자만 남은 상태로 정리(`grep`으로 `marquee_brand` 흔적이 코드 어디에도 없음을 확인).
- **2026-08-22(마퀴 패딩·line-height를 Figma 실측대로 수정)**: 사용자가 "마퀴 섹션 위아래 패딩 100px인데 안 맞는 것 같다"며 [Figma node 632:457](https://www.figma.com/design/xYrHj7h7ULDihhqhQmSrX2/%EC%86%A1%EB%AA%85%ED%9D%AC?node-id=632-457&m=dev) 확인 요청 — `get_design_context`로 대조한 결과 실제로 `.marquee_band`가 `padding: 100px 0 120px`(위 100 / 아래 120, 불일치)였는데 Figma는 `py-[100px]`(위아래 동일 100px)임을 확인해 `padding: 100px 0`으로 수정. 같은 대조에서 `leading-[1.5]`(line-height 1.5)도 기존에 안 들어가 있던 것을 발견해 `.marquee_track span`에 `line-height: 1.5` 추가. 참고로 Figma 원본 자체에는 구분자(•)가 없고 40px `gap`만으로 문구를 나눴는데, 구분자는 사용자가 가독성을 위해 명시적으로 요청해 추가한 것이라 그대로 유지함(Figma와의 의도적인 차이).
  - 검증: `index.html`의 `.marquee_band`에 임시 id를 붙여 새 격리 Chrome 창(포트 8941)에서 스크린샷으로 위아래 여백이 시각적으로 대칭인 것을 확인 후 임시 id 제거.
- **2026-08-22(WHY F45 KOREA 카드에 AOS 스크롤 등장 애니메이션 추가)**: 사용자가 [AOS 라이브러리 데모 사이트](https://michalsnik.github.io/aos/)를 보여주며 "이 사이트에 있는 스크롤 애니메이션을 WHY F45 KOREA 섹션 카드에 알아서 적용해줘"라고 요청(효과 선택은 자유 위임).
  - **라이브러리 선택**: PRD 16장은 GSAP/ScrollTrigger/Lenis/Swiper만 "조건 충족 시" 언급하고 AOS는 명시돼 있지 않지만, 사용자가 이 라운드에서 AOS를 직접 지정·요청했으므로 이번 건에 한해 추가함(요청 없이 임의로 추가한 라이브러리 아님). CDN(`cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/`)만 사용, 빌드 도구·패키지 매니저 도입 없음 — 기존 GSAP 도입 방식과 동일한 패턴.
  - **`transform: rotate()` 충돌 회피**: `.korea_row_k/o/r/e/a` 각각이 이미 자체 `transform: rotate(±2deg)`를 갖고 있어(카드가 살짝 기울어진 디자인), AOS가 같은 엘리먼트에 `translate` transform을 얹으면 서로 덮어써 회전이 사라지는 문제가 생김 — `.korea_row_X`를 직접 감싸는 새 래퍼 `<div data-aos="fade-up">`를 추가해 AOS는 래퍼에서만 opacity/translate를 다루고, 기존 회전은 안쪽 `.korea_row_X`에 그대로 유지되도록 분리함. `.korea_grid`(flex column, gap:4px)의 직계 자식이 래퍼로 바뀌었지만 래퍼가 스타일 없는 block 요소라 레이아웃에는 영향 없음.
  - **효과·타이밍(자유 위임분)**: 5개 카드 모두 `fade-up`(아래에서 위로 페이드인) 적용, 첫 카드만 delay 없음·나머지 4개는 `data-aos-delay="100"`(각 카드가 세로로 크게 떨어져 있어 스크롤 중 한 번에 한 카드씩만 뷰포트에 들어오므로, 그리드형 레이아웃에서 흔한 "0/100/200/300/400ms 순차 스태거"보다는 동일한 소폭 지연으로 등장에 살짝 여유를 주는 방식을 택함), `AOS.init({duration:700, easing:'ease-out-cubic', once:true, offset:80})`.
  - **`js/scroll_animations.js` 신규 작성**(기존 `js/modal.js`의 IIFE+방어적 체크 패턴과 통일): AOS의 `aos.css`가 `[data-aos^="fade"]`에 `opacity:0`을 **무조건**(초기화 여부와 무관하게) 거는 것을 CDN CSS 원본을 직접 확인해 파악 — `AOS.init()`이 실행되지 않으면 해당 엘리먼트가 영원히 `opacity:0`으로 숨겨진 채 남는 구조. 이에 따라 두 가지 방어 로직을 추가함: (1) `typeof window.AOS === "undefined"`(CDN 로드 실패)면 `data-aos` 속성을 직접 제거해 카드가 애니메이션 없이 즉시 보이도록 처리, (2) `prefers-reduced-motion: reduce`면 `AOS.init({..., disable:true})`로 초기화 — AOS 소스(`aos.js`)를 직접 확인해 `disable:true`일 때 AOS 스스로 모든 `data-aos*` 속성을 제거하는 내장 동작(`M()` 함수)이 있음을 확인했고, 이 내장 동작을 그대로 활용함(직접 속성을 지우는 중복 로직을 추가하지 않음).
  - `index.html`: `<head>`에 `aos.css` CDN 링크 추가, `</body>` 직전에 `aos.js` CDN 스크립트 + `js/scroll_animations.js` 스크립트 태그 추가(기존 GSAP 스크립트 바로 다음 순서).
  - 검증: 로컬 서버(포트 8952)를 새로 띄우고 격리된 새 Chrome 창에서 세 번째 카드(`RELATIONSHIPS`) 래퍼에 임시 `id="aos_test_anchor"`를 붙여 그 위치로 바로 이동한 뒤 스크린샷으로 확인 — RELATIONSHIPS·EXPERT COACHES 카드가 opacity:0으로 숨겨지지 않고 정상적으로(완전 불투명, 원래 회전 각도 유지, 레이아웃 깨짐 없음) 보이는 것을 확인, 이후 임시 id 제거. **다만 Apple Events로는 JS 실행·부드러운 점진적 스크롤을 재현할 수 없어(이번 세션에서도 재확인) URL 앵커로 "순간 이동"한 뒤의 최종 상태만 확인했고, 실제로 사용자가 마우스로 스크롤할 때 카드가 아래→위로 페이드인하며 "움직이는" 모습 자체는 이번에도 스크린샷으로 검증하지 못함** — 브라우저에서 직접 WHY F45 KOREA 섹션까지 천천히 스크롤해보며 각 카드가 순서대로 부드럽게 나타나는지, 속도감(0.7초)이 적당한지 확인 필요. 콘솔 에러 유무도 DevTools를 직접 열어야 확인 가능해 이번엔 확인하지 못함.
- **2026-08-22(히어로 섹션 스크롤 전환 효과 — GSAP ScrollTrigger 시도 후 되돌리고 순수 scroll 이벤트로 재구현)**: 사용자가 GSAP 커뮤니티에서 흔히 쓰이는 "pin + 스크롤 진행에 따라 scale·opacity 감소" 패턴 코드(다음 섹션이 pin된 패널을 덮으며 스크롤되는 효과, ScrollTrigger 기반)를 붙여넣고 "이런 느낌의 스크롤 애니메이션도 한 섹션에만 너가 알아서 조정해서 만들어줘"라고 요청.
  - **섹션 선택(자유 위임분)**: 히어로(`#hero`)를 선택 — 원본 코드가 여러 패널을 순회하며 "이전 패널이 축소·페이드되며 다음 패널이 덮어 올라오는" 효과였는데, 사이트에서 가장 시각적으로 무거운 히어로(영상 배경)가 스크롤 시작과 동시에 다음 섹션(WHY F45 KOREA)에 덮이며 사라지는 도입부 연출이 "이런 느낌"에 가장 잘 맞는다고 판단.
  - **1차 시도(pin 방식, 되돌림)**: 원본과 동일하게 `gsap.registerPlugin(ScrollTrigger)` + `pin:true, pinSpacing:false`로 히어로를 고정한 채 다음 섹션이 덮어 올라오게 구현 — 과정에서 두 가지 문제를 직접 재현·확인함:
    1. 다음 섹션이 히어로 위로 올라오려면 히어로가 다른 정적 배치 섹션들보다 뒤에 그려져야 한다고 판단해 `.hero`에 `z-index:-1`을 추가했는데, 실제로는 이게 히어로 전체(영상·타이틀·버튼)를 완전히 안 보이게 만드는 렌더링 버그를 일으킴(스크린샷으로 직접 재현·확인) — z-index 제거로 즉시 해결됨.
    2. z-index 문제 해결 후에도 `pin:true`가 문서 흐름을 재구성하면서 이 사이트가 이미 갖고 있는 **`#id` 앵커 이동이 전혀 동작하지 않는** 심각한 회귀가 발생 — `#app`/`#story`/`#locator` 등 어떤 해시로 URL을 열어도 스크롤이 히어로 위치에 멈춰 전혀 움직이지 않음을 반복 재현·확인함. 헤더 로고·"지점 찾기"·"체험권" 네비게이션은 이전 라운드에 이미 사용자가 확인한 핵심 기능이라 이 회귀는 그대로 둘 수 없다고 판단.
  - **2차 시도(pin 제거, ScrollTrigger 초기화를 window load로 지연, 부분적으로만 해결)**: `pin`을 빼고 스크롤 진행에 따라 scale·opacity만 tween하도록 단순화 — 그런데도 "URL에 `#locator`를 붙인 채 페이지를 처음 여는" 시나리오에서 여전히 앵커 이동이 안 되는 문제가 재현됨. ScrollTrigger가 초기화 시 트리거 위치를 다시 측정(`refresh`)하면서 스크롤 위치에 관여하는 게 원인으로 보여, 초기화 시점을 `DOMContentLoaded`에서 `window`의 `load` 이벤트(모든 리소스 로드 + 브라우저 네이티브 해시 이동이 끝난 뒤)로 미룸 — `#locator`는 해결됐지만, 같은 방식으로 `#guide`를 테스트하니 이번엔 목표 섹션을 지나쳐 `#app`(다음 섹션) 위치로 넘어가는 **다른 종류의 오작동**이 재현됨. 즉 pin 유무·초기화 시점과 무관하게 ScrollTrigger 플러그인 자체의 refresh 메커니즘이 이 사이트의 해시 앵커 이동과 근본적으로 충돌한다고 결론.
  - **최종 구현(ScrollTrigger 플러그인 완전 배제)**: ScrollTrigger CDN 스크립트 태그를 제거하고, `js/scroll_animations.js`에 순수 `scroll`/`resize` 이벤트 리스너 + GSAP core(`gsap.set`)만으로 동일한 시각 효과(스크롤 진행률에 따라 `scale` 1→0.85, `opacity` 1→0.25)를 재구현 — 이 방식은 스크롤 위치를 **읽기만** 하고 절대 쓰지 않으므로 앵커 이동과 원리적으로 충돌할 수 없음. `#hero`/`#locator`/`#guide`/`#app` 앵커를 URL에 직접 붙여 처음 여는 시나리오로 재검증해 모두 올바른 섹션에 정확히 도착하는 것을 확인(이전 두 번의 회귀가 모두 사라짐).
  - **PRD 16장과의 관계(보고)**: GSAP core는 기존에도 모달 트랜지션에 쓰이고 있었지만 ScrollTrigger 플러그인은 이번이 처음 시도였음 — 결과적으로 최종 구현에는 포함되지 않음(제거함). GSAP core만으로 원하는 시각 효과를 재현했으므로 "PRD 16장 조건 충족 전까지는 CSS·기본 JS로 구현" 원칙에서 크게 벗어나지 않음.
  - 검증: 로컬 서버(포트 8965)에서 격리 Chrome 창으로 `?nocache=N#hero`/`#locator`/`#guide`/`#app` 조합을 반복 테스트 — 최종 버전에서 4개 앵커 모두 올바른 섹션에 정확히 스크롤되는 것을 스크린샷으로 확인, 히어로가 스크롤 최상단(진행률 0)에서 원래 모습(전체 불투명·원래 크기)으로 정상 렌더링되는 것도 확인. **다만 이번에도 Apple Events로는 점진적 마우스 스크롤을 재현할 수 없어, 실제로 스크롤하는 도중 히어로가 매끄럽게 축소·페이드되는 "움직이는" 모습 자체와 최종 배율(0.85)·투명도(0.25) 값이 보기에 적당한지는 검증하지 못함** — 브라우저에서 직접 히어로 구간을 천천히 스크롤해보며 느낌을 확인해주시면 좋음, 마음에 안 들면 배율·투명도 값(`js/scroll_animations.js`의 `updateHeroTransform` 함수)만 조정하면 됨.
- **2026-08-22(히어로 스크롤 효과 방향 반전 — 축소→확대)**: 사용자가 "히어로가 줄어들면서 스크롤되는데 확대되면서 스크롤됐으면 좋겠다"고 요청.
  - `js/scroll_animations.js`의 `updateHeroTransform`에서 `scale: 1 - progress * 0.15`(1→0.85로 축소)를 `scale: 1 + progress * 0.15`(1→1.15로 확대)로 변경. `opacity`(1→0.25 페이드아웃)는 요청 범위가 아니라 그대로 유지.
  - **자유 판단으로 추가한 부분**: `gsap.set(hero, { transformOrigin: "50% 100%" })`를 추가함(기본값은 중앙 50% 50%). 확대는 축소와 달리 원점 기준으로 사방으로 커지는데, 중앙 기준이면 확대분만큼 히어로 하단이 아래로도 자라나 이미 스크롤로 올라와 있는 다음 섹션(#story) 콘텐츠 위를 불필요하게 더 덮게 될 것으로 판단 — 원점을 하단 중앙으로 옮겨 아래쪽 경계는 고정하고 위쪽(이미 스크롤로 화면 밖으로 나가는 방향)으로만 커지도록 함.
  - 검증: 로컬 서버(포트 8977)에서 `#hero`/no-hash/`#guide` 조합 재확인 — 스크롤 최상단에서 히어로가 원래 크기·불투명도로 정상 렌더링되는 것, `#guide` 앵커 이동이 여전히 정확히 동작하는 것(이번 변경이 scale 값·transform-origin만 건드려 위 항목에서 고친 앵커 충돌 문제와는 무관함을 재확인)을 스크린샷으로 확인. **이번에도 점진적 스크롤 중 실제로 커지는 모습·transform-origin을 하단으로 둔 것이 의도대로 자연스러워 보이는지는 스크린샷으로 검증하지 못함** — 직접 스크롤해보고 원점 위치나 확대 폭(현재 1.15배)이 어색하면 알려주시면 조정 가능.
- **2026-08-22(#guide 섹션에 "카드가 화면 중앙에서 잠시 멈췄다 넘어가는" 스크롤 효과 추가)**: 사용자가 GSAP `ScrollSmoother`(`smooth`/`effects`/`normalizeScroll`) + `ScrollTrigger`(`.shape`를 화면 중앙 도달 시 300px간 pin)를 조합한 참고 코드를 붙여넣고 "이런 느낌의 스크롤 애니메이션 추가해줘"라고 요청.
  - **구현 전 위험 보고 및 AskUserQuestion**: 바로 위 히어로 작업에서 GSAP ScrollTrigger의 `pin`이 이 사이트의 `#id` 앵커 이동을 반복적으로 깨뜨렸던 것을 직접 겪은 직후라, `ScrollSmoother`(페이지 전체 스크롤을 가상 스크롤로 감싸는 훨씬 더 침습적인 라이브러리 — sticky 헤더·`.guide_info`·직접 만든 히어로 효과·AOS 등 `window.scrollY`/`position:sticky`에 의존하는 기존 기능 전부와 충돌할 위험이 큼)를 그대로 쓰는 대신, 먼저 위험을 사용자에게 보고하고 "안전한 CSS 대안(추천)" vs "원본 그대로(ScrollSmoother+ScrollTrigger)" 중 선택하도록 확인 — **사용자가 "안전한 CSS 대안" 선택**.
  - **구현(순수 CSS, JS 없음)**: `.guide_card`(6개)를 각각 `.guide_card_pin` 래퍼로 감싸고, 래퍼에 `padding-bottom: 240px`(기존 `.guide_list`의 `gap:240px`를 대체 — 두 배로 겹치지 않도록 gap 제거)을 줘 카드 실제 높이보다 래퍼를 더 크게 만든 뒤, 안쪽 `.guide_card`를 `position:sticky; top:50%; transform:translateY(-50%)`로 전환 — 래퍼가 스크롤되는 240px 구간 동안 카드가 화면 정중앙에 붙박여 있다가 다음 카드로 자연스럽게 넘어감. `position:relative`→`sticky`로 바뀌어도 `.num`(절대 배치 숫자)의 기준점은 그대로 유지됨(sticky도 relative처럼 포지션드 컨테이닝 블록을 만듦). ScrollTrigger의 `pin:true`가 만드는 것과 시각적으로 비슷한 "화면 중앙에서 멈췄다 넘어가는" 느낌을 스크롤 위치를 읽거나 바꾸는 스크립트 없이(순수 CSS) 재현 — 이 사이트의 앵커 이동과 원리적으로 충돌할 수 없음.
  - **부수 수정**: `.guide_list`에 `.guide_info`와 동일한 `padding-top:200px` 추가 — 두 컬럼의 시작 기준선을 맞추기 위해 시도했던 것으로, 실제로는 아래 검증 과정에서 이 수정이 필수는 아니었던 것으로 보이지만(진짜 원인은 스크린샷 타이밍이었음, 아래 참고) 시각적으로도 두 컬럼이 자연스럽게 나란히 시작해 그대로 유지함.
  - **검증 중 발견한 큰 함정(중요, 다음에도 참고)**: 처음 `#guide`로 앵커 이동 테스트했을 때 엉뚱하게 03~04번 카드가 보여서 "sticky 계산이 잘못됐다"고 판단해 한참 원인을 찾았으나(`padding-top:200px` 추가 등 시도), 결국 진짜 원인은 **`html { scroll-behavior: smooth }`가 이미 걸려있는 상태에서, 이번 작업으로 `#guide` 섹션 높이가 약 1640px(200px + 240px×6) 늘어나면서 앵커로 점프할 때의 스무스 스크롤 애니메이션 자체가 이전보다 훨씬 오래 걸리게 됐고, 스크린샷을 찍은 시점(1.5~2.5초 대기)이 그 애니메이션이 채 끝나기 전 "스크롤 도중" 프레임을 찍은 것**이었음 — 대기 시간을 5초로 늘려 재확인하니 `#guide`는 정확히 01번 카드가, `#app`은 정확히 트라이얼 섹션이 중앙/정위치에 보임(둘 다 정상). **이 프로젝트에서 섹션 높이를 크게 늘리는 스크롤 효과를 추가할 때는, 앵커 이동 검증 시 스무스 스크롤이 다 끝날 때까지 대기 시간을 넉넉히(섹션이 커진 만큼) 늘려야 한다**는 교훈을 얻음 — 이후 검증부터 반영함.
  - 검증: 로컬 서버(포트 8991)에서 새 격리 Chrome 창(테스트 도중 실수로 사용자가 실제로 쓰던 기존 창을 재사용해버려 혼선이 있었음을 인지하고 이후 전부 `make new window`로 새 창을 명시적으로 만들어 진행) + 각 카드에 임시 id(`pin_test_1~6`)를 붙여 개별 카드가 화면 중앙에 정확히 붙박이는 것을 확인, 이후 임시 id 전부 제거(코드에 잔재 없음 grep으로 확인). 5초 대기 후 `#guide`(01번 카드 정중앙)·`#app`(트라이얼 섹션) 앵커 이동이 모두 올바른 위치로 도착하는 것도 재확인. **다만 이번에도 점진적 스크롤 중 카드가 실제로 "멈췄다가 넘어가는" 리듬이 자연스러운지, 정지 구간(240px)이 적당한 길이인지는 스크린샷(순간 이동)으로 검증하지 못함** — 직접 스크롤해보고 멈추는 느낌이나 카드 크기·정지 구간이 어색하면 알려주시면 조정 가능. 참고로 테스트 과정에서 `127.0.0.1:5501`로 열려 있던 Chrome 창을 정리 차원에서 닫았는데, 만약 그게 직접 열어두신 창이었다면 다시 Live Server로 열어주셔야 함.
- **2026-08-23(#phase 섹션에 카드 순환 "캐터필러" 효과 추가)**: 사용자가 GSAP **Flip** 플러그인 기반의 "Flip Caterpillar" 참고 코드(다음/이전 클릭 시 맨 앞 카드가 맨 뒤로, 혹은 그 반대로 자리를 옮기며 순환하는 캐러셀)를 붙여넣고 "#phase 섹션에 이 스크롤 애니메이션 느낌도 추가해줘"라고 요청.
  - **위험도 재판단(보고)**: 이번엔 앞의 두 건과 달리 위험 확인 질문을 따로 드리지 않고 바로 구현함 — Flip 플러그인은 ScrollTrigger/ScrollSmoother와 달리 스크롤 위치를 추적·재계산(refresh)하는 메커니즘이 전혀 없고, 순수하게 "버튼 클릭 시점의 DOM 변경 전/후 상태를 비교해 애니메이션"하는 방식이라 이 사이트의 `#id` 앵커 이동과 충돌할 이유가 구조적으로 없다고 판단했기 때문(앞의 두 건에서 문제가 된 것은 전부 스크롤 위치 관련 로직이었음).
  - **원본과의 차이(자유 판단)**: 원본은 `<img>` 하나만 다루고 매번 새 `<img>` 엘리먼트를 만들어 넣는 방식이었는데, 이 섹션의 카드는 썸네일+프로그램명+설명을 함께 가진 `.phase_card` 통짜 블록이라 카드 전체(`cloneNode(true)`)를 옮기도록 각색함. 원본의 "이동하는 카드를 그대로 날아가게 두지 않고, 반대쪽 끝에 복제본을 만들어 그 자리에서 페이드인시키고 원래 요소는 제자리에서 페이드아웃 후 제거"하는 핵심 기법(`Flip.from`의 `fade`/`absoluteOnLeave`/`onEnter`/`onLeave`)은 그대로 유지 — 7장짜리 카드 행 전체를 가로지르는 긴 이동이 아니라 "여기서 사라지고 저기서 나타나는" 느낌을 재현하기 위함.
  - `index.html`: `.phase_grid` 바로 아래에 `.phase_controls`(우측 정렬 이전/다음 버튼 2개, 기존 `#app` 트라이얼 캐러셀의 `.trial_arrow` 패턴과 동일한 시각 스펙으로 `.phase_arrow` 신규 클래스 작성 — 이름은 섹션에 맞게 새로 붙이되 스타일은 재사용) 추가, 기존 `left.png`/`right.png` 아이콘 재사용(새 에셋 없음). GSAP Flip CDN(`Flip.min.js`, gsap 3.12.5와 동일 버전) + `js/phase_carousel.js` 스크립트 태그 추가.
  - `css/style.css`: `.phase_controls`/`.phase_arrow` 신규 추가.
  - `js/phase_carousel.js` 신규 작성: 클릭한 방향의 가장자리 카드를 복제해 반대쪽 끝에 `scale:0/opacity:0`으로 삽입하고 원본엔 기존 `is_hidden`(`display:none`, `.branch_item`/`.trial_cta` 등에서 이미 쓰는 상태 클래스 재사용) 클래스를 붙인 뒤 `Flip.from()`으로 전체 카드의 위치 변화를 애니메이션 — 진입 카드는 `onEnter`에서 스케일·페이드 인, 이탈 카드는 `onLeave`에서 스케일·페이드 아웃 후 DOM에서 제거. 드래그로 스크롤해둔 위치가 카드 순서 변경과 어긋나 보이지 않도록 매 클릭마다 `track.scrollLeft = 0`으로 초기화. `prefers-reduced-motion`이면 Flip/클론 없이 카드만 즉시 재배치. **자체적으로 추가한 안전장치**: `onLeave`가 어떤 이유로든 실행되지 않아 `isAnimating` 플래그가 영영 `true`로 남아 버튼이 잠기는 사태를 막기 위해, 애니메이션 시간(0.6초)보다 넉넉한 1초 타이머로 한 번 더 플래그를 풀어주는 코드를 추가함(Flip의 leave 감지가 실제로 이 마크업에서 항상 작동하는지 클릭 테스트로 확인하지 못해 방어적으로 추가).
  - 검증: 로컬 서버(포트 9010) 격리 Chrome 창에서 `.phase_controls` 버튼이 카드 오른쪽 아래에 화살표 아이콘과 함께 정상 렌더링되는 것, 레이아웃 깨짐이 없는 것을 스크린샷으로 확인. **Apple Events로는 클릭 자체를 재현할 수 없어(반복 확인된 환경 제약) 버튼을 눌렀을 때 실제로 카드가 순환하는지, Flip 애니메이션이 매끄럽게 재생되는지, 복제·제거 로직이 의도대로 동작하는지는 전혀 확인하지 못함** — 코드 리뷰로 로직 흐름만 검증함. 사용자가 브라우저에서 직접 이전/다음 버튼을 여러 번 눌러보며 카드가 자연스럽게 순환하는지, 빠르게 연타했을 때 깨지지 않는지 확인 필요.
- **2026-08-23(되돌림)**: 사용자가 실제로 버튼을 눌러본 뒤 "제대로 적용이 안된 것 같아 그냥 전으로 돌려줘"라고 요청 — 바로 위 캐터필러 효과를 전체 되돌림. `js/phase_carousel.js` 삭제, `index.html`의 `.phase_controls`(이전/다음 버튼 2개) 마크업과 GSAP Flip CDN·`phase_carousel.js` 스크립트 태그 제거, `css/style.css`의 `.phase_controls`/`.phase_arrow` 규칙 제거. `git diff`로 `#phase` 섹션이 이 작업 이전 상태와 완전히 동일함(diff 없음)을 확인. 정확히 무엇이 "제대로 적용 안 됨"이었는지(클릭이 아예 안 먹었는지, 애니메이션이 이상했는지, 다른 문제였는지)는 구체적으로 전달받지 못해 원인 분석 없이 요청대로 전체 되돌림만 수행함 — 다시 시도할 경우 어떤 부분이 문제였는지 알려주시면 같은 실수를 피할 수 있음.
- **2026-08-23(#phase 카드 행 오른쪽 풀블리드 복원)**: 사용자가 "저번처럼 오버플로우 히든이 안 먹혀있어. 카드 오른쪽 부분은 넘치게 보여야 해"라고 요청. 문구만으로는 정확한 대상이 모호했으나, PROJECT_CONTEXT.md의 2026-08-08 "가로 스크롤바 제거" 항목(`.phase_grid`가 원래 `left:50%+width:100vw`로 양쪽 풀블리드였다가 가로 스크롤바 버그 때문에 `.wrap`(1440px) 안에 담기는 `width:100%`로 되돌렸던 기록, 그리고 그 항목에 남겨둔 "다시 시도하려면 `overflow-x:hidden`을 body에 추가하는 방식" 제안)와 정확히 일치해 이 건으로 판단하고 진행함.
  - `css/style.css`의 `body`에 `overflow-x: hidden;` 추가 — `.phase_grid`가 다시 오른쪽으로 풀블리드될 때 `100vw` 계산이 세로 스크롤바 폭만큼 실제보다 넓어져 생기는 1px 안팎의 오차를 페이지 레벨에서 안전하게 감춤(이전 버그의 재발 방지책).
  - `.phase_grid`를 `width:100%`에서 `width: calc(100% + 80px + max(0px, (100vw - var(--content-width) - 80px) / 2))`로 변경 — 왼쪽 시작 위치(=.wrap 콘텐츠 왼쪽 끝)는 그대로 두고 오른쪽으로만 뷰포트 끝까지 늘림. **처음 시도에서 발견한 실수**: `.section`(40px)과 `.wrap`(40px) 양쪽에 각각 좌우 패딩이 있다는 것을 빠뜨리고 `.wrap` 패딩(40px)만 계산해 첫 버전은 왼쪽·오른쪽 카드가 둘 다 어중간하게 잘려 보이는 잘못된 결과가 나왔음(스크린샷으로 확인) — `.section`의 패딩까지 포함해 "기존에 이미 올바르게 동작하던 `width:100%`" 값에 필요한 오른쪽 여백만 더하는 방식(`100% + 여백`)으로 다시 계산해 수정함.
  - 검증: 로컬 서버(포트 9021) 격리 Chrome 창에서 재확인 — 첫 번째 카드(PIPELINE)는 기존과 동일한 왼쪽 위치에서 시작하고, 화면 오른쪽 끝의 카드(THE NINES)가 잘린 채로 화면 밖까지 이어지는 것을 스크린샷으로 확인. 페이지 자체의 가로 스크롤바는 `body { overflow-x:hidden }`으로 인해 생기지 않을 것으로 판단되나, 실제 마우스 휠/트랙패드로 좌우 스크롤을 시도해 진짜로 페이지 레벨 가로 스크롤이 없는지는 이번에도 Apple Events 제약으로 재현하지 못함 — 사용자가 직접 페이지 하단 등에서 좌우 스크롤해보며 재확인 권장. 테스트 도중 `set URL of active tab of front window`로 사용자가 실제 쓰던 다른 Chrome 창(유튜브 검색 중)을 실수로 건드릴 뻔했으나 스크린샷에서 바로 확인해 이후 전부 `make new window`로 새 창을 명시적으로 만들어 진행함 — 페이지 이동은 실제로 반영되지 않고 그냥 스크린샷만 잘못 찍혔던 것이라 사용자 작업에 영향 없음.
- **2026-08-23(지점 찾기 지도를 정적 이미지 → 실제 확대/축소/이동 가능한 지도로 교체)**: 사용자가 "지도 섹션에 위치 기반 API 반영해서 확대, 축소, 이동 할 수 있게 만들어줘"라고 요청.
  - **PRD 충돌 보고**: PRD.md 5장(제외 범위)에 "지도 API 연동(실좌표 기반 검색·길찾기)은 포함하지 않습니다 — 디자인상 정적 지도 이미지를 사용합니다"가 명시돼 있어, 진행 전에 이 범위 제외와 충돌한다는 점을 먼저 보고함.
  - **API 키 문제 보고 및 AskUserQuestion**: 카카오맵/네이버맵/구글맵은 전부 사용자가 직접 개발자 등록 후 이 도메인용 키를 발급받아야 동작하는데(제가 임의로 만들어낼 수 없는 값), 어떤 지도로 진행할지 확인 질문 → **사용자가 "네이버지도" 선택**. 이후 Client ID·허용 도메인 등록·지점 좌표가 필요하다고 안내했으나, 사용자가 "무료로 진행할 수 있는 다른 방법 없어?"라고 재질문 → 가입·키 없이 바로 쓸 수 있는 **Leaflet(오픈소스 JS 지도 라이브러리) + OpenStreetMap 타일**로 방향을 바꿈(국내 지번 데이터가 카카오/네이버보다 상대적으로 부족하다는 트레이드오프는 보고 후 진행).
  - **지점 좌표 확보**: 이 사이트는 서버가 없는 정적 페이지라 브라우저에서 실시간 지오코딩(주소→좌표 변환)을 호출하기 어려워(CORS 제약), OpenStreetMap의 무료 Nominatim 서비스로 6개 지점 주소를 **구현 시점에 1회성으로** 조회해 위도/경도를 미리 구함(요청 정책 준수를 위해 1건씩 1.5초 간격, User-Agent 명시). 신사·보라매는 정확한 건물 번지까지 매칭됐고 나머지 4곳은 도로명 단위까지만 매칭돼(정확한 번지 데이터가 Nominatim에 없음) 실제 건물과 몇십~백여 미터 오차가 있을 수 있음 — 각 `.branch_item`에 `data-lat`/`data-lng`로 고정해둠.
  - `index.html`: Leaflet CSS/JS CDN 추가, `.map_image` 안의 `<img>`를 `<div id="locator_map">`로 교체(바깥 `.map_image` 컨테이너는 기존 940×627 크기·반응형 규칙을 그대로 재사용하기 위해 그대로 둠), 6개 `.branch_item`에 `data-lat`/`data-lng` 속성 추가.
  - `css/style.css`: `#locator_map`이 `.map_image` 컨테이너를 꽉 채우도록 크기 지정, Leaflet 기본 팝업 스타일을 프로젝트 톤(라벤더, Pretendard)에 맞게 재조정(`.leaflet-popup-content-wrapper` 등).
  - `js/locator_map.js` 신규 작성: `.branch_item`의 `data-lat/lng/name/address`와 `.branch_phone` 텍스트를 읽어 Leaflet 마커+말풍선(팝업)을 6개 모두 찍고 `fitBounds`로 전체가 한 화면에 들어오게 자동 프레이밍. **자체 판단으로 추가한 부분**: 지도가 페이지 안에 있으니 스크롤하다 지도 위에서 휠을 돌리면 페이지 스크롤이 아니라 지도 확대로 먹혀버리는 문제를 피하려고 `scrollWheelZoom`을 기본은 꺼두고 마우스가 지도 위에 있을 때만 켜지도록 처리(줌 +/- 버튼·더블클릭·핀치 확대는 항상 가능). Leaflet 로드 실패 시 "지도를 불러오지 못했습니다" 텍스트로 대체(빈 화면으로 깨지지 않게).
  - **범위에서 제외한 것(보고)**: 지점 목록 항목을 클릭하면 지도가 해당 마커로 이동하는 연동 기능도 구현해볼까 했으나, 요청받은 범위(지도 자체의 확대/축소/이동)를 벗어나는 추가 기능인 데다 `.branch_item`(현재 `<li>`)을 진짜로 클릭 가능하게 만들려면 키보드 접근성(포커스·Enter/Space 처리)까지 제대로 해야 해서 이번엔 넣지 않음 — 필요하시면 별도로 요청해주시면 구현 가능.
  - 검증: 로컬 서버(포트 9033) 격리 Chrome 창에서 `#locator`로 이동해 확인 — 실제 서울 지도(한강·강남·서초 일대)가 렌더링되고 6개 마커가 모두 실제 지점 위치(강남·서초·동작구 일대)에 합리적으로 분산돼 찍히는 것, 줌 +/- 컨트롤과 OpenStreetMap 저작권 표기가 정상 표시되는 것을 스크린샷으로 확인. **마커 클릭 시 팝업이 뜨는지, 실제 마우스 드래그로 이동되는지, 스크롤 휠 확대가 호버 시에만 켜지는지는 Apple Events로 클릭·드래그를 재현할 수 없어 확인하지 못함** — 코드 리뷰로 로직만 검증함. 사용자가 브라우저에서 직접 마커를 클릭해 팝업이 뜨는지, 드래그로 지도가 자연스럽게 움직이는지 확인 필요.

## 구현 중

- 없음(이번 라운드 범위 내 작업 완료)

## 확정된 UX 정책 (이번 세션에서 사용자 확인)

- 반응형: 2026-08-30 모바일 Figma 프레임을 받아 360~767px 모바일 레이아웃 구현 완료. 768px 태블릿 전용 레이아웃은 아직 범위 제외.
- hover/focus/active/disabled: 디자인에 정의 없음 → 브랜드 컬러(teal/lavender) 기준으로 직접 설계해 구현(사용자 승인)
- 지점 검색 결과 없음(빈 상태) UI: 별도 확인 전까지 미구현 — 현재는 필터링 로직만 동작하고 결과 0건이면 리스트가 비어 보임(안내 문구 없음)
- 마퀴/캐러셀 트랜지션: 마퀴는 기존 26s linear 유지, 캐러셀은 기본 즉시 전환(트랜지션 애니메이션 없음)으로 구현
- 소셜 아이콘: 5개(utube·facebook·twitter·instagram·in) 확정, design-analysis.md의 "6개" 서술은 부정확했던 것으로 확인
- "상세페이지 보기" 버튼: 실제 이동 없이 UI만(버튼 요소, 클릭 핸들러 없음)
- 헤더 햄버거/로그인: UI만 존재, 열림/닫힘 등 실제 토글 기능 없음
- 푸터 주소/전화/이메일: 확인된 사실 없음 → 원래 없는 정보이므로 넣지 않음(대표자·사업자등록번호만 표기)
- JS 파일 구조: `js/app.js`를 실제로 사용(인라인 스크립트 대신 분리 파일), `css/common.css`/`css/pages.css`/`js/storage.js`는 이번 랜딩 작업 범위에서는 비워둔 채 유지(추후 다른 페이지 작업 시 사용 예정)
- **(정정, 2026-08-07)** ~~검색어 없을 때 검색 버튼 disabled~~: 사용자가 "검색 버튼 같은거 클릭할 수 있게 해줘"라고 요청 — `locator_search_btn`이 사이트 전체에서 유일하게 `disabled` 속성을 가진(진짜로 클릭이 막히는) 버튼이었음을 grep으로 확인 후 제거. `index.html`에서 `disabled` 속성 삭제, `js/app.js`의 `initLocatorSearch()`에서 입력값에 따라 `searchBtn.disabled`를 토글하던 `handleInputChange()` 함수와 관련 호출 전부 제거. 검색어가 비어 있어도 클릭하면 `applyBranchFilter()`가 빈 키워드는 "전체 통과"로 처리해 정상 동작함(도시 필터만 적용됨).
- **(2026-08-09)** `#guide`(입문자 케어 가이드)의 1280~1439px 구간 가로 스크롤 위험은 지금 당장 손대지 않기로 확정 — 이미 확정된 "이번 라운드는 1280px만, 360/768은 추후"와 같은 선상에서, guide_info(500px)+guide_list(940px) 고정폭 레이아웃을 반응형으로 바꾸는 작업은 향후 360px/768px 반응형 작업과 함께 진행하기로 사용자가 결정함(AskUserQuestion으로 확인).
- **2026-08-07(헤더 로고 클릭 → 맨 위로 스크롤)**: 사용자가 "다 포함하고(다른 버튼들도) 헤더 로고 누르면 페이지 제일 위로 올라가게" 요청.
  - 로고: 기존 `<div class="logo">`를 기존 nav 링크(`<a class="nav_link" href="#story">` 등)와 동일한 패턴으로 `<a class="logo" href="#header">`로 변경(AGENTS.md "페이지 이동은 a 요소" 원칙과도 일치). `.logo`/`.logo img` CSS는 클래스 선택자라 태그를 바꿔도 그대로 적용되고, `.header`가 `display:flex`라 `<a>`도 다른 flex item과 동일하게 배치됨 — 재로드 스크린샷으로 레이아웃 변화 없음을 확인.
  - **"다 포함하고"(다른 버튼들)**: `grep`으로 다시 전수 확인한 결과 `disabled` 속성/토글은 검색 버튼 건이 마지막이었고 그 외에 실제로 "클릭이 막힌" 요소는 없음 — 상세페이지 보기·체험권 신청하기·로그인·스토어 버튼·푸터 소셜/약관 링크는 전부 이미 클릭 가능한 상태이고, 클릭해도 페이지 이동이나 별도 동작이 없는 건 PRD 5장에 명시된 의도된 범위 제외(실제 로그인/결제 없음, 지점 상세 화면 미존재, 실제 스토어·SNS URL 없음)라서 임의로 새 목적지를 만들지 않음 — 사용자에게 어떤 버튼에 어떤 동작(예: 특정 섹션으로 스크롤)을 원하는지 구체적으로 확인 필요하다고 보고함.
- **2026-08-08(헤더 nav·앱스토어 링크 목적지 확정)**: 위 보고에 대해 사용자가 구체적인 목적지를 지정 — 로고=페이지 최상단(이미 구현됨), "지점 찾기"=`#locator`(이미 구현됨), "체험권"=입문자 케어 가이드 섹션, App/Play Store 버튼=실제 스토어 URL.
  - `index.html`: nav "체험권" 링크를 `href="#hero"` → `href="#guide"`로 수정. `trial_store_buttons`의 App Store/Play Store `<a>` 2개의 `href="#"`를 실제 URL로 교체(`https://apps.apple.com/kr/app/f45-korea-new/id6751717648`, `https://play.google.com/store/apps/details?id=ie.zappy.fennec.fskorea&hl=ko`) — 외부 사이트로 나가는 진짜 링크라 `target="_blank" rel="noopener noreferrer"`도 함께 추가(새 탭으로 열리고 `window.opener` 보안 이슈 방지, 프로젝트에 이런 실외부링크가 처음 생겨서 새로 추가한 관례).
  - **부수적으로 발견/수정한 문제**: 사용자(또는 린터)가 이번 세션 중 `css/style.css`의 `.header`를 `position: sticky`로 바꿔둔 상태였는데(주석 처리돼 있던 걸 활성화), `scroll-padding-top` 보정이 없어서 앵커 이동(로고·지점 찾기·체험권 등 모든 `#id` 링크) 시 100px 높이의 sticky 헤더가 섹션 제목 위쪽을 가리는 문제가 있었음 — 지금 막 여러 앵커 링크를 새로 연결하는 작업이라 바로 영향을 미치는 문제라 판단해 `html { scroll-padding-top: 100px; }`을 추가해 같이 수정.
  - 검증: URL에 `#guide`/`#locator`를 직접 붙여 이동(앵커 클릭과 동일한 브라우저 네이티브 스크롤 동작)한 뒤 스크린샷으로 두 섹션 모두 제목이 sticky 헤더에 가리지 않고 완전히 보이는 것을 확인. 앱스토어/플레이스토어 버튼은 외부 사이트로 실제 이동하는 링크라 브라우저를 벗어나므로 클릭 자체(및 새 탭 오픈 여부)는 검증하지 못함 — href 값과 `target="_blank"` 속성이 정확히 들어간 것만 코드로 확인.
- **2026-08-08(로고 클릭 시 "한 칸씩만 이동" 버그 수정 + 네비 스무스 스크롤)**: 사용자가 "로고 누르면 한칸씩 올라가는데 히어로 있는 맨 위까지 가야 한다, 네비도 자연스럽게 이동되게"라고 지적.
  - **원인**: 로고가 `href="#header"`로 `<header>`(그 자체가 `position:sticky`) 요소를 가리키고 있었음 — sticky 요소는 스크롤해도 항상 화면 top:0에 붙어 있어 "이미 보이는" 상태로 취급되고, 브라우저의 앵커 이동 계산이 sticky 요소 기준으로는 정확한 절대 스크롤 위치를 못 잡아 조금씩만 움직이는 것으로 추정(sticky 타깃 앵커는 브라우저마다 처리가 불안정한 것으로 알려진 case).
  - **수정**: `js/app.js`에 `initLogoScrollTop()` 추가 — 로고 클릭 시 기본 앵커 동작을 막고(`preventDefault`) `window.scrollTo({top:0, behavior:'smooth'})`로 확실하게 절대 맨 위까지 이동시킴(sticky 타깃의 애매함을 우회). `href="#header"`는 JS가 실패했을 때를 위한 폴백으로 남겨둠.
  - **네비 스무스 스크롤**: `css/style.css`의 `html`에 `scroll-behavior: smooth` 추가 — 네비(`F45 KOREA`/`지점 찾기`/`체험권`) 등 모든 `#id` 앵커 링크가 순간 이동 대신 부드럽게 스크롤되도록 함(별도 JS 불필요, 순수 CSS). `prefers-reduced-motion: reduce` 사용자를 위해 같은 미디어쿼리 안에 `scroll-behavior: auto`로 되돌리는 규칙도 추가(기존 프로젝트의 모션 감소 대응 패턴과 통일), 로고의 JS `scrollTo`도 동일하게 reduced-motion이면 `behavior:'auto'`로 분기.
  - 검증: 재로드해 헤더·히어로가 정상 렌더링되는 것(레이아웃 깨짐 없음)만 스크린샷으로 확인함. **로고를 실제로 클릭했을 때 맨 위까지 제대로 가는지, 네비 클릭 시 스크롤이 부드럽게 보이는지는 Apple Events로 클릭 자체를 재현할 수 없어 이번에도 직접 확인하지 못함** — 브라우저에서 직접 눌러보고 여전히 "한 칸씩만" 움직이면 알려주시면 좋음(그 경우 sticky 타깃 앵커 문제가 아니라 다른 원인일 수 있음).
- **2026-08-08(로그인/체험권 신청 안내 모달 신규 디자인 + GSAP 애니메이션)**: 사용자가 "너 마음대로 로그인 화면이랑 체험권 신청하기 클릭하면 나오는 안내창 디자인해줄 수 있어? GSAP에서 괜찮은 애니메이션 있으면 추가해줘"라고 요청 — Figma 원본이 없는 화면이라 자유 디자인으로 진행.
  - **디자인 방향(보고)**: PRD 5장이 "실제 로그인/회원가입 기능은 만들지 않음", "실제 체험권 신청/결제 처리는 만들지 않음"을 명시하고 있어, 가짜 로그인 입력 폼이나 가짜 결제 폼을 새로 만들지 않음. 대신 사이트가 이미 일관되게 쓰고 있는 "실제 기능은 F45 Korea 앱에서" 흐름(#app 섹션이 이미 이 내용)을 그대로 재사용한 안내 모달로 디자인 — 로그인 트리거·체험권 트리거 모두 앱스토어/플레이스토어 링크로 안내. 사용자가 실제 입력 폼 형태를 원했다면 다른 디자인이었을 것이라 확인 필요.
  - `index.html`: 헤더 "로그인"을 `<a href="#">`에서 `<button id="login_trigger">`로 변경(AGENTS.md "동작은 button" 원칙 — 모달을 여는 건 페이지 이동이 아니라 동작). `hero_cta`에 `id="hero_cta"` 추가. 두 모달(`#login_modal`, `#trial_modal`)을 `</footer>` 뒤에 추가, 각각 `role="dialog" aria-modal="true" aria-labelledby` + 기본 `hidden` 속성으로 접근성과 초기 숨김 처리. 모달 안 App/Play Store 버튼은 기존 `.trial_store_btn`/`.trial_store_buttons` 클래스와 실제 스토어 URL을 그대로 재사용(새 컴포넌트 안 만들고 기존 것 재사용 — AGENTS.md 원칙).
  - `css/style.css`: `.modal_overlay`/`.modal_card`/`.modal_close`/`.modal_eyebrow`/`.modal_title`/`.modal_desc`/`.modal_store_buttons` 신규 추가(기존 색상 토큰 `--color-lavender`, `--radius-outline`, `--shadow-dropdown` 재사용). `.nav_link`가 이제 `<button>`에도 쓰이므로 `border:none;background:transparent` 추가(기존 `<a>`에는 영향 없음).
  - **GSAP 재도입**: 이전에 시도했다 "하나도 안 맞다"며 되돌렸던 GSAP를 이번엔 사용자가 다시 명시적으로 요청 — 지난번(스크롤 연동 ScrollTrigger+Draggable 무한 캐러셀)과 달리 이번엔 범위를 모달 열림/닫힘 트랜지션 하나로 최소화해 리스크를 줄임(gsap 코어만 CDN 추가, ScrollTrigger/Draggable 없음). `js/modal.js` 신규 작성: 배경 페이드 + 카드 scale·y·opacity를 `back.out(1.6)` 이징으로 열고 `power1.in`으로 닫음. GSAP 로드 실패나 `prefers-reduced-motion`이면 애니메이션 없이 즉시 열고 닫히도록 폴백 처리.
  - 접근성: 닫기 버튼(×)·배경 클릭·Escape 키로 닫힘, 열릴 때 닫기 버튼에 포커스 이동, 닫힐 때 원래 트리거로 포커스 복귀, 모달 열려있는 동안 `body.has_modal_open`으로 배경 스크롤 잠금. 완전한 포커스 트랩(Tab 순환 가둠)까지는 구현하지 않음(모달 안 포커스 가능 요소가 3개뿐이라 우선순위 낮다고 판단) — 필요하면 추가 가능.
  - 검증: `hidden` 속성을 임시로 지워 두 모달을 각각 강제로 열어 스크린샷으로 확인 — 배경 딤, 카드 스타일, 아이콘·문구·스토어 버튼 모두 의도대로 렌더링됨을 확인 후 원복. **실제 버튼 클릭으로 열고, GSAP 트랜지션이 실제로 재생되는지, Escape·배경 클릭으로 닫히는지는 Apple Events로 클릭을 재현할 수 없어 이번에도 확인하지 못함** — 브라우저에서 로그인/체험권 신청하기 버튼을 직접 눌러 확인 필요.
- **2026-08-08(가로 스크롤바 제거 + 히어로 100vh)**: 사용자가 "가로 스크롤바가 생기는데 너비값 100% 줘서 없애고, 히어로는 100vh로 화면에 꽉 차게 해줘"라고 요청.
  - **가로 스크롤바 원인**: `css/style.css` 전체에서 `100vw`를 쓰는 곳은 `.phase_grid`(이번 PHASE 카드 캐러셀) 한 곳뿐이었음 — `left:50%; width:100vw; margin-left:-50vw;`로 `.wrap`(1440px 중앙 정렬 컨테이너) 밖으로 화면 끝까지 풀블리드시키는 트릭을 쓰고 있었는데, `100vw`는 세로 스크롤바 폭까지 포함해 실제 보이는 너비보다 넓게 계산되는 널리 알려진 버그가 있어 그 차이만큼 가로 스크롤바가 생기고 있었음.
  - **수정**: `.phase_grid`를 `width:100%`(부모 `.wrap` 기준)로 바꾸고, 더 이상 필요 없어진 `left:50%`/`margin-left:-50vw`/`padding-left: max(40px, calc(...))` 풀블리드 보정 규칙을 함께 제거. **디자인 영향(보고)**: 이 카드 캐러셀이 더 이상 화면 오른쪽 끝까지 풀블리드로 이어지지 않고, 다른 섹션들과 동일하게 `.wrap`(1440px) 폭 안에 담기도록 바뀜 — 가로 스크롤바 제거를 우선한 결정이라 원래 풀블리드 느낌을 유지하고 싶으시면 다른 방식(예: `overflow-x: hidden`을 `body`에 추가해 1px 안팎 오차를 감추는 방식)으로 다시 시도 가능.
  - **히어로 100vh**: `.hero`의 `aspect-ratio: 3/2`를 `height: 100vh`(+`width:100%`)로 교체. 헤더가 `position:sticky`(100px)라 헤더+히어로 합계가 뷰포트보다 100px 커지지만, 요청하신 값을 그대로 적용함 — 헤더 높이까지 정확히 맞추려면 `height: calc(100vh - 100px)`가 더 정확할 수 있어 필요하면 조정 가능.
  - 검증: 재로드 스크린샷으로 히어로가 첫 화면을 꽉 채우는 것, PHASE 섹션 카드가 다른 섹션과 같은 좌측 정렬선에 맞게 담기는 것을 확인. 사용자가 이미 열어둔 것으로 보이는 Chrome DevTools 스크린샷에서 `html, body`에 `width:100%`가 정상 적용된 것도 확인. 다만 실제 가로 스크롤바가 "완전히" 사라졌는지는 스크린샷상 결정적 증거(스크롤바 자체는 OS/브라우저 설정에 따라 스크린샷에 거의 안 보일 수 있음)로 단정하기 어려워, 사용자가 직접 페이지 하단에서 좌우로 스크롤해보며 재확인 권장.
- **2026-08-09(가로 스크롤 진짜 원인 추가 발견 — 섹션의 숨은 좌우 패딩)**: 위 phase_grid 수정 이후에도 사용자가 "아직도 가로 스크롤돼"라고 재지적 — `#locator`/`#guide`/`#app`가 `padding-top`/`padding-bottom`만 덮어쓰고 `.section`의 좌우 40px 패딩은 그대로 물려받고 있었는데, 그 안의 `.locator_inner`/`.guide_inner`/`.trial_inner`는 전부 "자체 패딩 없이 `max-width:1440px`"로 설계돼 있어서 이 40px×2 패딩 때문에 실제로는 뷰포트가 1520px는 돼야 안 잘리는 상태였음(특히 `#guide`는 `guide_info`(500px)+`guide_list`(940px)가 `flex-shrink:0`라 줄어들지도 않아 이게 실제 원인이었음).
  - `css/style.css`에서 세 섹션 모두 `padding-left:0; padding-right:0;` 추가 — 1440px부터 정상적으로 딱 맞도록 수정.
  - 사용자에게 확인한 결과(AskUserQuestion): (1) 본인 노트북에서는 더 이상 가로 스크롤이 안 보이지만 다른(데스크탑) 환경에서는 여전히 보인다고 답변 — 뷰포트가 1440px 미만인 환경일 가능성이 높음(정확한 창 폭은 미확인). (2) `#guide`가 1280~1439px 구간에서는 이번 수정 이후에도 여전히 위험이 남아있다는 것을 안내했고, **"지금은 놓아두기"로 확정** — 이미 합의된 "이번 라운드는 1280px만, 360/768은 추후"와 같은 선상에서, 1280~1439px 처리도 나중에 360/768 반응형 작업과 함께 처리하기로 함.
  - **후속(같은 날)**: 사용자가 데스크탑에서 여전히 보인다며 DevTools "Dimensions: Responsive 1242 x 1235"가 찍힌 스크린샷을 전달 — 1242라는 숫자가 정확히 그 반응형 시뮬레이션 값과 일치해, "실제 창 크기가 아니라 데스크탑 Chrome의 DevTools 반응형 모드(기기 도구모음)가 켜져서 1242px를 강제로 시뮬레이션하고 있는 것 아니냐"고 가설을 세우고 확인 요청(AskUserQuestion) → **사용자가 반응형 모드를 끄고 DevTools도 닫은 뒤 재확인, "안 보임"으로 확정 답변** — 데스크탑 이슈는 실제 레이아웃 버그가 아니라 DevTools 반응형 모드(1242px, 이미 1280px 미만이라 이번 라운드 범위 밖)가 원인이었음이 최종 확인됨. 가로 스크롤 버그는 이것으로 완전히 해결.

## 사용 중인 라이브러리

- **GSAP 3.12.5**(코어만, CDN): `js/modal.js`의 로그인/체험권 신청 모달 열림·닫힘 트랜지션, `js/scroll_animations.js`의 히어로 스크롤 축소·페이드(`gsap.set`)에 사용. ScrollTrigger·Draggable 플러그인은 없음(ScrollTrigger는 히어로 효과 구현 중 시도했다가 앵커 이동 충돌 문제로 완전히 제거함 — 위 2026-08-22 항목 참고. 이전에 되돌렸던 `#app` 갤러리 시도와도 무관 — 그건 순수 CSS transition으로 남아 있음). 빌드 도구·패키지 매니저는 여전히 없음(CDN `<script>` 태그로만 로드).
- **AOS 2.3.4**(CDN): WHY F45 KOREA 섹션 5개 카드의 스크롤 등장(`fade-up`) 애니메이션에 사용(`js/scroll_animations.js`). 사용자가 이번 라운드에서 직접 지정·요청함. `AOS.init()` 설정과 CDN 로드 실패/reduced-motion 방어 로직은 위 2026-08-22 항목 참고.
- ~~GSAP Flip 3.12.5~~: #phase 섹션 카드 순환("캐터필러") 효과에 시도했으나(2026-08-23), 사용자가 "제대로 적용이 안된 것 같다"며 되돌려 달라고 요청해 전체 제거함 — 현재 사용 안 함.
- **Leaflet 1.9.4**(CDN, OpenStreetMap 타일): 지점 찾기 지도의 실제 확대/축소/이동에 사용(`js/locator_map.js`). 카카오맵/네이버맵/구글맵은 API 키가 필요해 사용자가 "무료로 가능한 다른 방법" 요청 → 가입·키 없이 쓸 수 있는 조합으로 대체함(위 2026-08-23 항목 참고). 지점 좌표는 OpenStreetMap Nominatim으로 1회성 조회한 값을 `.branch_item`의 `data-lat`/`data-lng`에 고정.

## 저장 데이터

- 없음(localStorage 미사용, 도시/검색 필터는 세션 내 DOM 상태로만 유지)

## 알려진 문제 / 남은 확인 사항

- **Figma 재확인 완료(헤더·히어로·WHY F45 KOREA·지점 찾기·앱 설치)**: `node 632:378`(header), `node 632:394`(hero), `node 632:400`(WHY F45 KOREA), `node 632:570`(지점 찾기), `node 632:705`(앱 설치)를 실측 대조 완료. PHASE 섹션 제목 카피, 가이드 03단계("식사") 설명 문구, 푸터 등 나머지 섹션은 아직 대조하지 않았으므로 확인 필요.
- 지점 찾기 "우측 세로 라인" 요소(Figma `Line2`, map_txt 우측 5px 지점에 147px 높이): 정확한 색상·용도(스크롤바 힌트로 추정)를 100% 확인하지 못해 브라우저 네이티브 스크롤바를 얇은 lavender 색으로 스타일링하는 것으로 재해석해 구현함 — 디자인과 정확히 같은 그래픽은 아님.
- **PRD 5장과의 의도적 불일치**: PRD.md는 "지도 API 연동은 포함하지 않고 정적 지도 이미지를 사용"하도록 명시돼 있었으나, 2026-08-23에 사용자가 명시적으로 요청해 실제 인터랙티브 지도(Leaflet+OpenStreetMap)로 교체함 — 문서상 범위와 실제 구현이 달라진 상태이니 PRD를 갱신하거나(사용자 확인 필요) 이 항목을 최신 기준으로 참고할 것.
- **지점 좌표 정확도**: OpenStreetMap Nominatim으로 1회성 조회한 값이라 신사·보라매 2곳만 정확한 건물 번지까지 매칭됐고 나머지 4곳(교대·역삼·청담·강남)은 도로명 단위까지만 매칭됨 — 실제 건물과 몇십~백여 미터 오차 가능. 정확한 좌표를 알고 계시면 각 `.branch_item`의 `data-lat`/`data-lng`만 교체하면 됨.
- **브라우저 실측 방법 변경**: 이번 환경엔 여전히 chromium-cli·Playwright가 없지만, 사용자 PC의 실제 Chrome(VS Code Live Server, `127.0.0.1:5500`)을 macOS `osascript`(Chrome을 새 탭/URL로 열기)와 `screencapture`로 직접 캡처해 시각적으로 확인하는 방식을 이번 라운드에 사용함. Apple Events를 통한 JS 실행은 Chrome/Safari 모두 기본적으로 막혀 있어 `console.error` 등은 이 방법으로 직접 읽지 못함(콘솔 탭을 스크린샷으로 열어보는 방식으로 우회 가능하나 이번엔 레이아웃 확인에 집중함).
- 히어로 배경 비디오: 실제 Chrome에서 자동재생되는 것까지 스크린샷(연속 캡처 프레임이 서로 다름)으로 확인함.
- WHY F45 KOREA 각 행의 정확한 픽셀 오프셋(Figma의 `pl-20`, `pl-60`, `pr-100` 등 절대 좌표)은 근사치로 구현함 — 디자인 자체가 카드마다 손으로 배치한 듯한 "느슨한" 레이아웃이라 완전히 동일한 px까지는 맞추지 않았음, 필요시 정밀 조정 가능.
- 360px 모바일 레이아웃은 구현·검증 완료. 768px 태블릿 전용 레이아웃은 아직 없음.
- 가이드 섹션의 "좌측 타이틀 sticky + 우측 카드 쌓임" 스크롤 연출은 구현하지 않음(design-analysis.md도 추정으로만 기록, 이번 세션에서 별도 확인/지시 없었음) — 현재는 세로 목록으로만 표시
- guide 카드 "우측 상단 128px 숫자" 스펙은 폰트 크기 96px로 근사 구현(940px 카드 폭 대비 128px 숫자가 비율상 과도하게 커 보여 시각적으로 조정 — Figma 재확인 시 정확한 값으로 교체 필요)
- **앱 설치(`#app`) 애니메이션은 GSAP 시도 후 되돌리고 CSS 전환으로 재구현**: 1차로 GSAP(seamless-loop cards) 스크립트를 적용했으나 "하나도 안 맞다"는 피드백으로 되돌린 뒤, 사용자와 방향(전환 방식·자동재생 여부)을 다시 정하고 라이브러리 없이 CSS transition + `setInterval`로 재구현함(위 "구현 완료" 항목 참고). 자동재생 간격(7초), 전환 시간(0.9초), 스케일 값(0.94→1)은 임의로 정한 근사치라 실제 느낌을 사용자가 보고 조정 요청할 수 있음.
- 오른쪽 텍스트 슬라이드 애니메이션의 이동 폭(24px)·아웃 시간(280ms)도 임의로 정한 근사치 — 폰의 스케일+페이드(0.9초)와 나란히 봤을 때 속도감이 맞는지는 사용자가 실제로 봐야 판단 가능.

## 다음 작업

1. Figma 재확인 범위를 나머지 섹션(PHASE 제목 카피, 가이드, 푸터)으로 확장해 카피·수치 대조
2. 키보드 Tab 순서·focus-visible, 콘솔 에러(F12로 직접) 확인 — Apple Events 경로로는 콘솔 로그를 못 읽으므로 사용자가 직접 개발자 도구를 열어 확인하는 편이 정확함
3. 768px 태블릿 전용 디자인이 확정되면 태블릿 레이아웃 추가 구현
4. 지점 검색 빈 상태 UI 디자인 확정되면 반영
5. `#app` 자동재생 간격(현재 7초)·전환 속도(폰 0.9초/텍스트 아웃 0.15초+인 0.75초)·스케일 정도(현재 0.94)·오른쪽 슬라이드 이동폭(현재 24px)이 사용자 취향과 맞는지, 왼쪽·오른쪽이 실제로 "같이 움직인다"고 느껴지는지 브라우저에서 직접 확인 후 필요시 `js/app.js`/`css/style.css` 값 조정
6. 호버 시 자동재생 일시정지가 실제로 원하는 동작인지 사용자가 브라우저에서 직접 확인. ~~06단계 "체험권 신청하기" CTA 클릭 핸들러 없음~~ → 2026-08-08에 클릭하면 `trial_modal`이 열리도록 연결됨(아래 참고)
7. 브라우저 탭에 실제로 파비콘 아이콘이 보이는지 사용자가 직접 확인(스크린샷 해상도로는 탭 아이콘이 잘 안 보여 파일 경로만 검증함) — 캐시 때문에 안 보이면 강력 새로고침(Cmd+Shift+R) 필요할 수 있음
8. `favicon-og-set/meta-tags.html`에 있는 오픈그래프(카카오톡/페이스북 공유 미리보기)·트위터 카드 태그는 이번 요청 범위가 아니라 미적용 — 필요하면 요청
9. 로그인/체험권 신청 모달의 내용 방향(앱스토어 안내 vs 실제 입력 폼)이 사용자가 원한 게 맞는지 확인 필요 — Figma 시안 없이 임의로 정한 디자인이라 "이게 아니다" 싶으면 다시 요청
10. 모달 버튼(로그인, 체험권 신청하기 2곳)을 실제로 클릭해 GSAP 열림/닫힘 애니메이션이 매끄러운지, Escape·배경 클릭으로 잘 닫히는지 사용자가 직접 확인(Apple Events로 클릭 재현 불가해 코드 리뷰로만 확인함)
11. PHASE 카드 캐러셀이 이제 화면 오른쪽 끝까지 풀블리드로 이어지지 않고 `.wrap`(1440px) 폭 안에 담기는 것으로 바뀜 — 이 변경이 괜찮은지, 아니면 풀블리드 느낌을 다른 방식으로 유지하고 싶은지 확인 필요
12. 히어로 `height: 100vh`가 헤더(sticky, 100px)와 합쳐졌을 때 정확히 원하는 "화면에 꽉 참" 느낌인지 확인 — 필요하면 `calc(100vh - 100px)`로 조정 가능
13. ~~페이지 전체 가로 스크롤바~~ → **완전히 해결·종결(2026-08-09)**. `#locator`/`#guide`/`#app`의 숨은 좌우 40px 패딩 제거로 실제 원인 수정, 이후 "데스크탑에서도 보인다"던 재발 건은 원인을 추적한 결과 실제 창 크기가 아니라 데스크탑 Chrome DevTools의 반응형 모드(1242px 시뮬레이션)가 켜져 있던 것으로 확인됨(반응형 모드+DevTools 끄고 재확인해 "안 보임" 답변받음) — 실제 레이아웃 버그 아님.
14. `#guide`(입문자 케어 가이드)는 768~1439px 구간에서 PC 고정폭 500px+940px 때문에 가로 스크롤 위험이 남아있음. 767px 이하는 이번 모바일 규칙에서 단일 컬럼으로 해결됨.

## 마지막 검증 결과

- **2026-08-30(히어로 5번 신규 이미지 430×600)**: `hero_frame_05_generated_430x600.jpg`를 직접 열어 정확히 9명(뒷줄 8명·앞줄 1명)이 있고 모든 인물의 얼굴·전신·신발이 잘리지 않으며 F45 벽면 로고가 포함된 것을 육안 확인. 실제 파일 크기는 정확히 430×600px임.

- **2026-08-30(히어로 4번 신규 이미지 430×600)**: `hero_frame_04_generated_430x600.jpg`를 직접 열어 네 인물의 얼굴·상체·어깨동무와 F45 로고가 포함되고 하체 전체가 노출되지 않는 상반신 단체 구도임을 육안 확인. 실제 파일 크기는 정확히 430×600px임.

- **2026-08-30(히어로 3번 신규 이미지 430×600)**: `hero_frame_03_generated_430x600.jpg`를 직접 열어 남성의 전신·배틀로프, 오른쪽 여성의 덤벨 동작, 배경 운동 인원과 F45 벽면 로고가 포함된 것을 육안 확인. 실제 파일 크기는 정확히 430×600px임.

- **2026-08-30(히어로 2번 신규 이미지 430×600)**: `hero_frame_02_generated_430x600.jpg`를 직접 열어 두 인물의 머리·몸·손·발과 케틀벨이 잘리지 않고 모두 포함된 것을 육안 확인. F45 체육관 배경, 파란 바닥, 코칭 동작이 유지됐으며 실제 파일 크기는 정확히 430×600px임.

- **2026-08-30(히어로 신규 이미지 430×600)**: 생성된 `hero_frame_01_generated_430x600.jpg`를 직접 열어 남성 트레이너 중심의 악수 구도, 여성 회원 2명, F45 체육관 배경과 로고가 유지된 새로운 사진임을 육안 확인. 실제 파일 크기도 정확히 430×600px임을 확인함.

- **2026-08-30(히어로 1번 430×600)**: `hero_frame_01_430x600.jpg`를 직접 열어 트레이너와 로고가 포함된 세로 구도를 육안 확인하고, 파일의 실제 픽셀 크기가 정확히 430×600임을 확인함. 기존 `hero_frame_01.jpg`는 변경하지 않음.

- **2026-08-30(히어로 5번째 이미지)**: 영상 후반부 4.6~7.0초를 0.2초 간격 컨택트 시트로 비교해 5.4~5.6초의 다인원 장면을 확인하고, 5.5초 부근 프레임을 직접 열어 9명이 F45 로고 앞에 모여 있는 장면임을 육안 확인. 최종 파일은 1620×1080이고 4번과 5번 체크섬이 서로 달라 중복 이미지가 아님을 확인함.

- **2026-08-30(히어로 자동 캐러셀)**: 430×900 인앱 브라우저에서 이미지·인디케이터가 각각 5개이고 인디케이터가 CTA 상단에 배치된 것을 확인. 초기 1번에서 5.2초 후 2번으로 이미지와 활성 점이 함께 전환됐고, 네 번째 점 클릭 시 이미지·점 모두 index 3으로 동기화됨. 전환 완료 후 화면에 보이는 이미지는 1장이고 브라우저 오류 로그는 0건. 360px 히어로 자체는 가로 넘침이 없었으며, 768·1280px의 페이지 전체 `scrollWidth:1440px`은 기존 PROJECT_CONTEXT의 알려진 `#guide` 고정폭 문제로 이번 히어로 변경 범위와 무관함.

- **2026-08-30(히어로 꽉 채우기 + 프레임 추출)**: 430×900 인앱 브라우저에서 히어로와 이미지가 각각 415×600px, `object-fit:cover`, 페이지 `scrollWidth === clientWidth`로 확인됨. 연결된 `hero_frame_01.jpg`의 자연 크기는 1620×1080이고 브라우저 오류 로그는 0건. 추출한 5장도 모두 1620×1080이며 서로 다른 체크섬을 확인함. `object-fit:cover` 특성상 화면 비율에 따라 이미지 좌우 일부가 잘리는 것은 의도된 동작임.

- **2026-08-30(속도·정지 이미지 후속)**: 430×900 브라우저에서 hero 내부 video 0개, `hero_mobile.jpg` 로드·600px `contain`, PHASE computed `scroll-behavior:auto`/`scroll-snap:none`, GUIDE transition-duration 0.4s를 확인. 히어로 실제 캡처에서 선택한 코치·F45 로고 장면과 타이틀/CTA가 정상 표시됨을 육안 확인했고 console error/warning 0건. 모바일 터치 속도의 체감은 실제 기기에서 최종 확인 필요.

- **2026-08-30(모바일 후속 조정)**: 430×900 인앱 브라우저에서 hero video `object-fit:contain`, PHASE `scroll-snap:none`/`scroll-behavior:smooth`, branch_list clientHeight 291·scrollHeight 582·overflow-y auto 및 스크롤바 색상, 초기 GUIDE 카드 6개 opacity 0을 확인. GUIDE 상단까지 스크롤했을 때 첫 카드만 `is_visible`/opacity 약 1, 나머지 5장은 0으로 유지돼 한 장씩 제자리 페이드인되는 것을 확인. 콘솔 error/warning 0건. 실제 iOS Safari의 손가락 관성 감각과 커스텀 스크롤바 외형은 이 환경에서 직접 확인하지 못함.

- **2026-08-30(모바일 전체)**: 로컬 서버와 인앱 브라우저의 실제 viewport override로 430×900·360×800을 확인. 430px에서 섹션 높이는 header 64 / hero 600 / story 2589(Figma 2587) / marquee 106 / phase 691(Figma 695) / locator 1146(Figma 1160) / guide 1253(Figma 1252) / app 763(Figma 762) / footer 296px로 원본과 거의 일치함. 360px에서 `documentElement.scrollWidth === clientWidth`로 페이지 가로 스크롤이 없음을 확인했으며, 화면 밖 요소는 의도된 마퀴 트랙과 PHASE 내부 가로 스크롤 카드뿐이었음. 히어로 동영상 세로 크롭, 도시 드롭다운 열기/닫기, 앱 02번 dot 전환, 모바일 검색 Enter 후 역삼 1개 필터 결과를 실제 조작해 확인. 브라우저 error/warning 로그 0건. 768px 태블릿 및 실제 모바일 기기 Safari/Chrome 터치 조작은 이번 라운드에서 확인하지 못함.

- **2026-08-06(지점 찾기·앱 설치 재구현)**: `python3 -m http.server`/VS Code Live Server(`127.0.0.1:5500`)로 정적 서빙 후 사용자 실제 Chrome을 `open -a`+`osascript`(URL 이동)+`screencapture`로 캡처해 두 섹션을 Figma 스크린샷과 나란히 육안 대조함 — 지점 찾기(드롭다운·검색창·검색 버튼·주변 매장 찾기·지점 리스트 스크롤·지도)와 앱 설치(폰 목업·01/06 번호·타이틀·스토어 버튼·좌우 화살표·인디케이터) 모두 레이아웃·색상·타이포가 일치함을 확인. `git diff`로 `trial_left/trial_tit/trial_steps/trial_cta` 등 제거 대상 클래스가 HTML·CSS 어디에도 남지 않았음을 grep으로 확인.
- **2026-08-06(GSAP 갤러리 되돌림)**: `index.html`/`css/style.css`/`js/app.js`를 GSAP 적용 이전 상태로 수동 복원하고 `js/trial_gallery.js`를 삭제 → grep으로 `gallery`/`cards`/`drag-proxy`/`trial_gallery` 등 GSAP 관련 흔적이 하나도 남지 않았음을 확인.
- **2026-08-06(CSS 전환 애니메이션 추가)**: Chrome을 계속 포그라운드에 둔 채(백그라운드 탭은 `setInterval`이 스로틀링되어 타이머가 거의 안 도는 것을 이번에 확인함) `#app` 섹션을 로드 직후 1회, 약 7.3초 뒤 1회 총 2장 캡처 — 초기 상태(01/06, 첫 번째 화면)에서 자동재생으로 03/06(로그인 화면)까지 정상 진행되고 dot pill·번호가 함께 갱신되는 것을 확인. 다만 전환 애니메이션이 진행 중인 "중간 프레임"(크로스페이드 도중)을 정확히 캡처하지는 못해 스케일+페이드 효과 자체의 부드러움은 스크린샷으로 검증하지 못함 — 코드상 CSS transition 속성은 확인함.
- 호버 시 자동재생 정지/재개는 Apple Events로 실제 마우스 진입/이탈 이벤트를 보낼 수 없어(System Events 접근성 권한 없음) **직접 확인하지 못함** — 코드 리뷰로 `mouseenter`/`mouseleave` 리스너가 올바르게 연결된 것만 확인.
- **2026-08-06(Figma 컴포넌트로 6단계 문구·버튼 확정 + 우측 슬라이드 애니메이션)**: `#app`을 로드한 뒤 자동재생이 진행되는 동안 여러 차례 스크린샷을 찍어 01/06(F45 설치 화면, App/Play Store 버튼)·02/06(도시 검색 화면, 버튼 없음)·03/06(로그인 화면, 버튼 없음)·05/06(크레딧 구매 화면, 버튼 없음)·06/06(예약 화면, "체험권 신청하기" CTA) 다섯 단계에서 폰 화면·문구·버튼 표시 여부가 모두 Figma 컴포넌트 라이브러리에서 확인한 값과 정확히 일치하는 것을 확인함(04/06만 스크린샷 타이밍이 안 맞아 못 찍었으나 나머지 5단계가 전부 같은 코드 경로로 일관되게 맞아 04단계도 동일하게 동작할 것으로 판단).
- 오른쪽 텍스트의 슬라이드+페이드 전환이 실제로 얼마나 부드러운지(전환 "도중" 프레임)는 이번에도 정확히 캡처하지 못해 스크린샷으로 검증하지 못함 — 전/후 상태가 올바르다는 것만 확인.
- **2026-08-06(타이밍·위치 수정)**: 세로 위치는 03/06(로그인, 버튼 없음) 상태를 재로드 후 스크린샷으로 확인 — 콘텐츠가 이전보다 위쪽에 자리하고 아래쪽 여백이 커진 것을 확인(Figma pb-171 적용 후 기대한 방향과 일치). 타이밍 동기화는 자동재생 tick 직후(약 6.7초 지점) 0.25초 간격으로 3연속 캡처를 시도했으나 세 장 모두 전환이 이미 끝난 "정지 상태"만 잡혀서, 실제 전환 "도중" 프레임으로 왼쪽·오른쪽이 같이 움직이는지는 **이번에도 스크린샷으로 직접 확인하지 못함** — 코드 리뷰로 (1)폰의 `is_active` 토글과 텍스트의 아웃 클래스 추가가 같은 동기 실행 틱에서 일어나는 것, (2) 아웃 0.15s+인 0.75s=0.9s로 폰과 총 길이가 같아진 것만 확인함. 바로 직전 라운드(이 수정 전)에 우연히 잡은 중간 프레임 스크린샷에서 "폰은 다 나타났는데 텍스트는 아직 반투명"한 어긋남을 실제로 목격했었고, 그 원인(텍스트가 처음 절반을 화면 밖에서 보냄)을 코드로 수정했지만 수정 후 버전의 중간 프레임은 재확인하지 못한 상태 — 사용자가 직접 눈으로 보고 여전히 어긋나 보이면 구체적으로(예: "텍스트가 늦게 나타난다" 등) 알려주시면 좋음.
- 실행 명령: 파일시스템 기준 에셋 경로 전수 대조(전부 OK), `avmediainfo`로 hero.mp4 코덱 확인(H.264/AAC), 사용자 실제 Chrome(Live Server 5500)을 `osascript`+`screencapture`로 캡처해 헤더 로고 크기·히어로 영상 재생·WHY F45 KOREA 레이아웃을 시각적으로 직접 확인함
- 360px/768px 화면 비교: **미실시**(사용자 결정으로 이번 범위 제외), 1280px은 스크린샷으로 확인
- 키보드 Tab 순서·focus-visible, 콘솔 에러 로그: **미실시**(Apple Events로 JS 실행이 막혀 있어 화면 캡처로는 확인 불가 — 사용자가 직접 F12로 확인 필요)
- 확인하지 못한 부분: 위 "알려진 문제 / 남은 확인 사항" 전체, `#app` 호버 일시정지 실제 동작, 수정된 전환 애니메이션의 실제 동기화 여부(중간 프레임 재확인 못함)
- **2026-08-06(2~5단계도 1·6단계처럼 상단 정렬로 재수정)**: 재로드 후 01단계("01/06" 시작 y좌표)와 자동재생으로 도달한 04단계("04/06", 버튼 없는 화면) 스크린샷을 나란히 비교 — 두 단계의 텍스트 시작 위치가 거의 동일함을 확인(이전엔 04단계가 눈에 띄게 아래에 있었음). 02·03·05·06단계는 개별 재확인하지 않았으나 모두 같은 CSS 규칙(`align-items:stretch` + `justify-content:flex-start`)을 타므로 동일하게 상단 정렬될 것으로 판단 — 필요하면 사용자가 화살표로 전체를 넘겨보며 확인 권장.
- **2026-08-07(도시 드롭다운)**: 닫힘 상태를 재로드 스크린샷으로 확인해 "도시 선택" 텍스트가 한 줄로 정상 표시됨을 확인(처음엔 146px 고정폭 탓에 두 줄로 깨지는 걸 실제로 목격 → width:100% 방식으로 수정 후 재확인). 열림 상태는 우연히 캡처된 프레임에서 버튼·목록의 좌우 테두리가 정확히 일치하고, "전체" 강조·나머지 흰 배경·남부만 하단 모서리가 둥근 것을 확인 — 다만 이 열림 상태 스크린샷은 의도적으로 클릭해서 연 게 아니라 우연히 그 순간 캡처된 것이라(Apple Events로 실제 클릭을 못 함), **직접 클릭해서 열고 닫는 전환 자체는 검증하지 못함** — 사용자가 브라우저에서 직접 클릭해 열림/닫힘이 매끄러운지 확인 필요.
- SEO 메타 태그: 브라우저 렌더링에 영향 없는 `<head>` 텍스트라 별도 스크린샷 확인 없이 `git diff`로 오타·중복 여부만 확인함.
- **2026-08-07(파비콘)**: `favicon/` 아래 8개 파일(`favicon.svg/ico`, `favicon-16/32.png`, `apple-touch-icon.png`, `icon-192/512.png`, `site.webmanifest`)이 실제로 존재하는지 `ls`로 전수 확인. 새로고침 후 콘솔 에러 없이 페이지가 로드되는 것은 스크린샷으로 확인했으나, **브라우저 탭에 실제로 아이콘이 표시되는지는 스크린샷 해상도 한계로 육안 확인하지 못함** — 사용자가 직접 탭을 봐서 확인 필요(캐시로 안 보이면 강력 새로고침 권장).
- **2026-08-07(검색 버튼 항상 클릭 가능하게)**: `grep`으로 `disabled` 속성/토글 로직이 `locator_search_btn` 한 곳에만 있었음을 확인 후 제거. 재로드 스크린샷으로 버튼이 더 이상 흐리게(전역 `button:disabled{opacity:.45}`) 보이지 않고 진한 라벤더 배경의 활성 상태로 표시되는 것을 확인함 — 실제 클릭해서 지점 목록이 필터링되는지는 Apple Events로 클릭을 재현할 수 없어 확인하지 못함(코드상 `applyBranchFilter` 로직 자체는 이번에 건드리지 않음).
- **2026-08-08(로그인/체험권 신청 모달)**: 두 모달 각각 `hidden` 속성을 임시로 지워 강제로 연 상태를 스크린샷으로 확인 — 배경 딤(어둡게), 카드 라운드·그림자, 닫기(×) 버튼, eyebrow/title/desc 문구, App/Play Store 버튼(아이콘 포함)이 모두 의도대로 렌더링되는 것을 확인 후 `hidden` 원복. **버튼을 실제로 클릭해서 여는 것, GSAP 열림/닫힘 애니메이션(scale+fade)이 실제로 재생되는 것, 닫기 버튼·배경 클릭·Escape 키로 닫히는 것, 포커스 이동/복귀는 전부 Apple Events로 클릭·키 입력을 재현할 수 없어 확인하지 못함** — 코드 리뷰로 이벤트 리스너 연결과 GSAP 호출부만 확인함. 사용자가 직접 로그인·체험권 신청하기(히어로+06단계 두 곳) 버튼을 눌러 실제 동작을 확인해 주셔야 함.
