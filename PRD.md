# F45 KOREA PRD

최종 요구사항 기준: 2026-09-20. 이번 Final Completion 요청에 따라 기존의 로그인·회원가입·지점 상세 제외 범위를 갱신합니다.

## 1. 제품 개요

F45 코리아의 브랜드, 트레이닝 프로그램, 지점과 첫 방문 절차를 소개하는 반응형 포트폴리오 웹사이트입니다. 브라우저 안에서 회원가입·로그인 흐름을 시연하고, 지점 상세 정보와 실제 F45 앱 설치 경로를 제공합니다.

## 2. 문제 정의

- 처음 방문한 사용자가 F45의 특징과 이번 PHASE 프로그램을 빠르게 이해하기 어렵습니다.
- 지점별 주소·전화번호와 첫 방문 준비 정보를 한 흐름에서 확인할 수 있어야 합니다.
- 포트폴리오에서 가입·로그인·로그아웃의 입력 검증과 상태 변화를 실제로 체험할 수 있어야 합니다.

## 3. 목표 사용자

- F45 체험을 고려하는 20~40대 및 그룹 피트니스에 관심 있는 방문자
- 모바일·태블릿·데스크톱 브라우저 사용자
- 사이트의 반응형 구성, 인터랙션, 접근성과 프런트엔드 구현을 살펴보는 포트폴리오 평가자

## 4. 제품 목표

- WHY F45 KOREA의 5가지 특징과 PHASE 7종을 이해할 수 있습니다.
- 도시·검색어 필터를 사용하고 6개 지점의 상세 정보로 이동할 수 있습니다.
- 체험 안내 모달과 App Store·Play Store 링크를 통해 실제 앱으로 이동할 수 있습니다.
- 데모 회원가입·로그인·세션 유지·로그아웃과 헤더 상태 변화를 경험할 수 있습니다.
- 기존 F45 색상·폰트·이미지·버튼 스타일을 유지하고 모든 지정 화면 폭에서 사용할 수 있습니다.

## 5. 제외 범위와 데모 한계

- 실제 서버 인증, 사용자 DB, 백엔드, 외부 인증 서비스, 이메일·SMS 인증, OAuth 및 비밀번호 찾기는 구현하지 않습니다.
- 실제 결제, 멤버십 관리, 수업 예약 및 체험권 구매·신청 접수는 제공하지 않습니다.
- 데모 데이터는 현재 브라우저의 해당 사이트 저장소에만 남습니다. 다른 기기와 동기화되지 않으며 사이트 데이터를 삭제하면 사라집니다.
- 비밀번호 해시를 사용하더라도 브라우저 소유자가 데이터를 변경할 수 있으므로 실제 서비스의 인증·접근 제어를 보장하지 않습니다.
- 새 지도 API, 위치 권한 요청 및 실시간 길찾기는 없습니다. 지도는 기존 참고 이미지이며, ‘주변 매장 찾기’는 기존의 검색·도시 필터 초기화 동작을 유지합니다.
- 확인되지 않은 지점 운영시간·가격·코치·수업표·시설·후기는 작성하지 않습니다.

## 6. 디자인 기준

- 기존 [Figma 디자인](https://www.figma.com/design/xYrHj7h7ULDihhqhQmSrX2/송명희?node-id=632-377&m=dev), `design-analysis.md` 및 프로젝트의 현재 디자인 토큰을 재사용합니다.
- 배경 `#f8f9fa`, 본문 `#0a0913`, teal `#008ba3`, lavender `#5b5ceb`, 기존 radius·shadow와 로컬 Pretendard·Montserrat 폰트를 유지합니다.
- 새 로그인·회원가입·상세 화면은 승인된 추가 범위입니다. 기존 운동·브랜드 이미지를 재사용하며 특정 지점의 실제 내부 사진이라고 설명하지 않습니다.
- 외부 임의 이미지, 새 팔레트, 패키지 설치 또는 전체 디자인 재작성은 하지 않습니다.

## 7. 핵심 기능

- 홈: 기존 Hero 영상·모바일 이미지와 dots, WHY 카드, PHASE 휠·드래그 스크롤, 도시·지점 검색, Guide, 앱 6단계 캐러셀을 유지합니다.
- 체험 CTA: 홈과 상세에서 동일한 앱 설치 안내 모달을 엽니다. 실제 신청 정보를 수집하지 않습니다.
- 회원가입: 이름·이메일·휴대전화·비밀번호·비밀번호 확인·필수 동의 2개를 검증하고 사용자 레코드를 저장합니다.
- 로그인: 가입한 이메일·비밀번호를 검증하고 세션을 만듭니다. 이메일 저장은 선택 사항이며 비밀번호는 저장하지 않습니다.
- 공통 헤더: 비로그인 상태의 로그인 링크, 로그인 상태의 이름·로그아웃 버튼을 데스크톱과 모바일에 동일하게 표시합니다.
- 모바일 메뉴: 버튼·Escape·외부 클릭·내부 링크로 열고 닫으며 스크롤 잠금과 1024px 이상 전환 시 초기화를 제공합니다.
- 지점 상세: 하나의 `pages/detail.html?branch=...`에서 허용된 6개 지점을 표시합니다.

## 8. 화면 목록과 목적

| 화면 | 경로 | 내용과 주요 행동 |
|---|---|---|
| 홈 | `index.html` | 브랜드·프로그램 소개, 지점 필터·상세 이동, 가이드, 앱 설치·체험 안내 |
| 회원가입 | `pages/signup.html` | 입력·동의 검증, 데모 계정 생성, 로그인 화면으로 이동 |
| 로그인 | `pages/login.html` | 로그인, 이메일 저장, 회원가입 이동 |
| 지점 상세 | `pages/detail.html?branch=gyodae` 등 | 주소·전화·참고 지도, 트레이닝·첫 방문 안내, 앱 설치·체험 안내 |
| 로그인된 상태의 인증 화면 | 동일한 로그인·회원가입 경로 | 폼 대신 사용자 이름, 홈 이동 및 로그아웃 제공 |
| 잘못된 지점 상태 | 누락·잘못된 `branch` | 헤더·푸터 유지, 지점 정보 없음 안내, 홈·지점 찾기 이동 |

Header/Footer는 각 HTML에 직접 포함합니다. 하위 페이지의 홈 링크는 `../index.html`과 `#story`·`#locator`·`#guide`를 사용합니다. 목적지가 확인되지 않은 푸터 소셜·정책 항목은 클릭되지 않는 안내로 표시합니다.

## 9. 사용자 흐름

1. 홈에서 브랜드·프로그램을 확인하고 도시 또는 지점명·주소로 지점을 찾습니다.
2. ‘상세페이지 보기’ 링크로 지점의 주소·전화번호와 첫 방문 안내를 확인합니다.
3. 체험 CTA에서 앱 설치 안내를 확인하고 필요한 경우 실제 스토어로 이동합니다.
4. 데모 계정이 필요하면 회원가입 화면에서 필수 입력·동의를 완료합니다.
5. 가입 성공 후 `sessionStorage`로 전달된 이메일과 성공 안내가 로그인 화면에 표시되며 비밀번호 입력으로 포커스가 이동합니다.
6. 로그인하면 홈으로 이동하고 헤더에 이름과 로그아웃이 표시됩니다. 새로고침·페이지 이동 후에도 세션이 유지됩니다.
7. 로그아웃은 현재 페이지를 유지하며 세션만 삭제합니다. 가입 데이터와 저장한 이메일은 유지됩니다.

## 10. 입력 검증과 화면 상태

| 항목 | 규칙 |
|---|---|
| 이름 | 앞뒤 공백 제거, 2~30자, 숫자만 입력 금지 |
| 이메일 | 앞뒤 공백 제거·소문자 정규화, 이메일 형식, 중복 가입 금지 |
| 휴대전화 | `010`으로 시작하는 11자리, 입력 중 `010-0000-0000` 표시, 숫자만 저장 |
| 비밀번호 | 공백 없이 영문·숫자·특수문자를 포함한 8~20자 |
| 비밀번호 확인 | 원문과 완전히 일치 |
| 동의 | 이용약관·개인정보 수집 동의 모두 필수, 마케팅 동의 없음 |
| 로그인 | 이메일 형식과 비밀번호 공백 검증, 미가입 이메일·틀린 비밀번호 오류 안내 |

- 입력 오류는 필드 옆의 구체적인 한국어 문구와 `aria-invalid`·`aria-describedby`로 전달하고 첫 오류 필드에 포커스를 둡니다.
- 처리 중 제출 버튼을 비활성화하고 `aria-busy`·라이브 상태 안내를 갱신합니다. Enter 제출과 중복 제출 방지를 지원합니다.
- 로그인·회원가입 버튼은 JavaScript가 기능을 준비한 뒤 활성화합니다. JavaScript가 없으면 안내를 표시하고 인증 제출을 제공하지 않습니다.
- JSON 손상·잘못된 타입·필수 필드 누락은 해당 저장 키를 초기화합니다. 저장소 접근·용량 및 Web Crypto 오류는 사용자에게 안내합니다.
- 지점 검색 결과가 없으면 빈 상태 문구를 표시합니다. 검색어가 없어도 검색 버튼은 사용할 수 있습니다.
- 비밀번호 표시·숨김 버튼은 버튼 이름과 `aria-pressed`를 갱신합니다.

## 11. 데이터와 저장

지점 상세의 단일 데이터 소스는 `js/branch_data.js`의 읽기 전용 `F45Branches`입니다. 허용 slug는 `gyodae`, `yeoksam`, `sinsa`, `cheongdam`, `gangnam`, `boramae`입니다. 홈은 JavaScript 없이도 지점 목록과 링크를 읽을 수 있도록 기존 HTML을 유지하며 데이터 일치를 검증합니다.

| 저장 위치 | 키 | 값 |
|---|---|---|
| localStorage | `f45_demo_users_v1` | 사용자 레코드 배열 |
| localStorage | `f45_demo_session_v1` | 현재 세션 |
| localStorage | `f45_saved_email_v1` | 선택적으로 저장한 정규화 이메일 |
| sessionStorage | `f45_signup_email_v1` | 가입 직후 로그인에 전달할 이메일 |
| sessionStorage | `f45_signup_success_v1` | 가입 완료 표시 문자열 `true` |

사용자 레코드는 `id`, `name`, `email`, `phone`, `passwordHash`, `salt`, `termsAccepted`, `privacyAccepted`, `createdAt`으로 구성합니다. 세션은 `userId`, `name`, `email`, `loggedInAt`만 포함합니다.

- 비밀번호는 Web Crypto의 PBKDF2·SHA-256, 120,000회 반복, 사용자별 무작위 16바이트 salt, 256비트 파생 키로 처리합니다. salt와 해시는 base64로 저장하며 원문을 저장하지 않습니다.
- ID는 `crypto.randomUUID()` 또는 암호학적 난수로 만든 UUID v4를 사용합니다.
- 모든 저장 데이터를 읽을 때 스키마·타입·값을 검증합니다. 세션은 실제 저장 사용자와 대조합니다. `localStorage.clear()`는 사용하지 않습니다.
- 저장 이름·이메일과 지점명은 `textContent`로 표시합니다. 지점 쿼리는 허용 목록과 정확히 대조하며 원문을 HTML·title에 반영하지 않습니다.
- 도시·검색 필터는 DOM 상태이며 새로고침하면 초기화됩니다.

## 12. 개발 조건

- HTML·CSS·Vanilla JavaScript 정적 사이트로 유지하며 GitHub Pages의 저장소 하위 경로에서 동작하도록 상대 경로를 사용합니다.
- package.json·빌드 과정·프레임워크·백엔드·새 인증 서비스는 추가하지 않습니다.
- `css/style.css`는 홈, `css/common.css`는 공통 요소, `css/pages.css`는 인증·상세 레이아웃을 담당합니다.
- `js/storage.js`는 검증·저장·해시·세션, `js/auth.js`는 폼·헤더·메뉴, `js/branch_data.js`와 `js/branch_detail.js`는 지점 데이터와 렌더링을 담당합니다.
- 기존 `js/app.js`·`js/modal.js`·`js/scroll_animations.js`를 유지하고 `js/interactions.js`에 추가 인터랙션을 둡니다.

## 13. 명명 규칙

- CSS class와 HTML id는 snake_case, 상태는 `is_active`·`is_open`·`is_selected`, 오류는 `has_error`를 사용합니다.
- JavaScript 변수·함수는 camelCase, 불리언은 is·has·can·should, 이벤트 함수는 handleXxx 형식입니다.
- 전역 고정 상수만 UPPER_SNAKE_CASE를 사용합니다.

## 14. 반응형 기준

| 구분 | 검수 viewport |
|---|---|
| 모바일 | 360×800, 375×812, 390×844, 430×932 |
| 태블릿 | 768×1024, 1024×1366 |
| 데스크톱 | 1280×800, 1440×900, 1920×1080 |

- 모든 페이지는 `width=device-width, initial-scale=1.0`을 사용합니다. 작은 화면을 430px로 강제 확대하는 스크립트는 사용하지 않습니다.
- 전체 가로 스크롤·겹침·텍스트 잘림이 없어야 하며 `document.documentElement.scrollWidth <= window.innerWidth`를 계측합니다.
- 인증 화면은 데스크톱에서 이미지·폼을 나누고 모바일에서는 폼을 우선한 1열로 표시합니다.
- 모바일 메뉴는 1024px 미만에서 사용하며 데스크톱 전환 시 열린 상태와 스크롤 잠금을 해제합니다.
- 터치 영역은 44px 이상, 인증 입력·주요 버튼은 48px 이상을 기준으로 합니다.

## 15. 접근성

- header·nav·main·section·footer를 사용하고 동작은 button, 페이지 이동은 a로 구분합니다.
- 폼에 visible label·정확한 autocomplete·오류 연결·상태 안내를 제공하며 키보드 Tab·Enter·focus-visible을 유지합니다.
- 모바일 메뉴는 `aria-expanded`·`aria-controls`·열기/닫기 이름을 갱신하고 Escape 닫기와 포커스 복귀를 지원합니다.
- 모달은 dialog·aria-modal·제목 연결, 닫기 버튼 초기 포커스, Tab 순환, 배경 비활성화·스크롤 잠금, Escape·overlay 닫기·원래 버튼 포커스 복귀를 제공합니다.
- 의미 있는 이미지에는 alt, 장식에는 빈 alt를 사용합니다. 상태를 색상만으로 전달하지 않습니다.
- reduced motion에서 콘텐츠를 즉시 표시하고 포인터 효과·등장 효과·자동 전환을 줄이거나 멈추되 수동 조작은 유지합니다.

## 16. 인터랙션 라이브러리와 범위

- 기존 GSAP Core 3.12.5와 AOS 2.3.4를 고정 버전으로 재사용하며 중복 로드하지 않습니다.
- ScrollTrigger·Lenis·ScrollSmoother·pin·scroll hijacking과 React Bits 패키지는 추가하지 않습니다.
- 새 등장은 IntersectionObserver와 GSAP Core로 한 번 실행합니다. WHY 카드의 기존 AOS와 같은 요소를 중복 제어하지 않습니다.
- Hero 두 줄 등장, 섹션 제목 등장, 인증·상세 이미지 등장, Spotlight Card, Magnetic CTA를 적용합니다.
- React Bits의 BlurText/FadeContent·SpotlightCard/GlareHover·Magnet은 시각 원리만 참고하고 현재 HTML/CSS/JS로 구현합니다.
- Spotlight와 Magnet은 1024px 이상·fine pointer·hover 가능·모션 감소 미사용일 때만 리스너를 등록합니다. Magnet은 버튼 내부 span만 최대 6px 이동하고 키보드 포커스에서는 초기화합니다.
- 화면 크기·모션 설정 변경 시 리스너·애니메이션을 정리합니다. CDN·JavaScript가 실패해도 본문이 영구적으로 숨겨지지 않아야 합니다.
- 기존 앱 캐러셀은 CSS transition과 Vanilla JavaScript를 유지합니다. GSAP 캐러셀로 교체하지 않습니다.

## 17. 검증 방법과 Final QA

- `python3 -m http.server 8000`으로 localhost에서 검사합니다. 설치된 Playwright·Chrome을 활용하며 저장소에 테스트 의존성을 추가하지 않습니다.
- 14장의 9개 viewport에서 홈·로그인·회원가입·상세의 전체 화면 계측과 시각 검수를 진행합니다.
- 회원가입의 빈 값·이름·이메일·휴대전화·비밀번호·확인·동의·중복·정상 가입·이메일 전달·원문 미저장을 검사합니다.
- 로그인의 빈 값·형식·미가입 이메일·틀린 비밀번호·성공·이메일 저장/해제·새로고침/페이지 이동·로그아웃·이미 로그인된 화면·손상/차단된 저장소를 검사합니다.
- 6개 지점과 누락·존재하지 않는 값·인코딩된 특수문자·스크립트 문자열·중복 query를 검사합니다. 전화·메타·상대 링크·이미지를 확인합니다.
- 헤더·메뉴·앵커·Hero dots/CTA·PHASE wheel/drag·도시/검색/빈 상태·Guide·앱 캐러셀·모달의 기존 동작을 실제로 조작합니다.
- 키보드 포커스·모달·reduced motion·touch·resize·orientation·CDN 실패·no-JS 폴백을 확인합니다.
- 콘솔 오류·미처리 예외/Promise·내부 404·MIME·중복 id·끊긴 내부 링크를 확인합니다.
- PASS는 실제 실행한 항목에만 기록하고 미실행·실기기 한계를 구분합니다. 결과는 `PROJECT_CONTEXT.md`에 기록합니다.

## 18. 완료 조건

- 필수 기능과 주요 기존 동작이 정상이며 페이지 실패·인증 실패·지점 실패·모바일 overflow·헤더/모달/앵커 오류 등 Critical FAIL이 없어야 합니다.
- 실제 사용한 에셋·상대 경로·반응형·접근성·상태·CDN 폴백을 확인합니다.
- `git diff --check`와 작업 상태 확인 후 단일 최종 커밋을 만듭니다.
- 작업 브랜치는 `feature/f45-final-completion`, 커밋 메시지는 `feat: complete F45 auth branch detail and interactions`입니다.
- 작업 브랜치만 push하고 main에 병합하거나 Pull Request를 생성하지 않습니다. 사용자의 기존 변경은 보존합니다.
- 최종 보고에 기준 HEAD·최종 SHA·push·변경 파일·검증·미실행 항목·한계·preview·git 상태를 구분합니다.
